const APP_SIGNATURE="JETPRO-PAUL-ALAY-2026";let state={projectName:"Untitled Project",factors:[],positions:[],incumbents:[],evaluations:{},clusters:[],salaryGrades:[],unsaved:!1},currencySymbol="$";function setCurrency(t){if("custom"===t){const t=prompt("Enter custom currency symbol:",currencySymbol);null!==t&&t.trim()&&(currencySymbol=t.trim());const e=document.getElementById("currency-select");let a=e.querySelector('option[value="__custom__"]');a||(a=document.createElement("option"),a.value="__custom__",e.insertBefore(a,e.lastElementChild)),a.textContent=currencySymbol+" (custom)",e.value="__custom__"}else currencySymbol=t;state.currencySymbol=currencySymbol,markUnsaved();const e=document.querySelector(".page.active");if(e){const t=e.id.replace("page-","");"salary"===t&&renderSalary(),"ranking"===t&&renderRankingTable(),"positions"===t&&renderPositionsTable(),"dashboard"===t&&renderDashboard(),"analytics"===t&&renderAnalytics()}toast(`Currency set to ${currencySymbol}`,"success")}let editingFactorId=null,editingPositionId=null,currentEvalPositionId=null,positionSortKey="title",positionSortAsc=!0,rankSortKey="totalScore",rankSortAscending=!1,pendingImportData=[],charts={};const COLORS=["#4f8ef7","#00d4ff","#f5c842","#2ecc8a","#ff4f6b","#9b6dff","#ff8c42","#ff6bc4","#4fefb0","#ffa07a"];function toggleTheme(){const t=document.documentElement.classList.toggle("light");localStorage.setItem("jeb_theme",t?"light":"dark"),document.getElementById("theme-label").textContent=t?"Light":"Dark",document.getElementById("theme-icon-dark").style.display=t?"none":"",document.getElementById("theme-icon-light").style.display=t?"":"none";const e=document.querySelector(".page.active");e&&"page-analytics"===e.id&&renderAnalytics(),e&&"page-dashboard"===e.id&&renderDashboard(),e&&"page-salary"===e.id&&(destroySalaryCharts(),setTimeout(renderSalaryCharts,50))}function applyTheme(){"light"===localStorage.getItem("jeb_theme")&&(document.documentElement.classList.add("light"),document.getElementById("theme-label").textContent="Light",document.getElementById("theme-icon-dark")&&(document.getElementById("theme-icon-dark").style.display="none"),document.getElementById("theme-icon-light")&&(document.getElementById("theme-icon-light").style.display=""))}function getChartColors(){const t=document.documentElement.classList.contains("light");return{grid:t?"#dde1f0":"#1e2842",tick:t?"#3a4a72":"#8a9bbf",legend:t?"#3a4a72":"#8a9bbf"}}function navigate(t){document.querySelectorAll(".page").forEach(t=>t.classList.remove("active")),document.querySelectorAll(".nav-item").forEach(t=>t.classList.remove("active"));const e=document.getElementById("page-"+t),a=document.getElementById("nav-"+t);e&&e.classList.add("active"),a&&a.classList.add("active");document.getElementById("page-title-text").textContent={dashboard:"Dashboard",factors:"Factor Builder",positions:"Position Library",incumbents:"Incumbent Library",workspace:"Evaluation Workspace",ranking:"Job Ranking Dashboard",clustering:"Level Clustering",salary:"Salary Structure Designer",analytics:"Visual Analytics",spanofcontrol:"Span of Control Analysis",payequity:"Pay Equity Analysis",reports:"Consulting Report",datamanagement:"Data Management"}[t]||t,"dashboard"===t&&renderDashboard(),"positions"===t&&renderPositionsTable(),"incumbents"===t&&renderIncumbentsTable(),"ranking"===t&&renderRankingTable(),"workspace"===t&&renderWorkspace(),"clustering"===t&&renderClustering(),"salary"===t&&renderSalary(),"analytics"===t&&renderAnalytics(),"spanofcontrol"===t&&renderSpanOfControl(),"payequity"===t&&renderPayEquity(),"datamanagement"===t&&renderDataManagement(),updateWorkflowState()}function openModal(t){document.getElementById(t).classList.add("open")}function closeModal(t){document.getElementById(t).classList.remove("open")}document.querySelectorAll(".modal-overlay").forEach(t=>{t.addEventListener("click",e=>{e.target===t&&t.classList.remove("open")})});let tempLevels=[];function openAddFactorModal(t=null){if(editingFactorId=t,tempLevels=[],document.getElementById("modal-factor-title").textContent=t?"Edit Factor":"Add Compensable Factor",t){const e=state.factors.find(e=>e.id===t);e&&(document.getElementById("f-name").value=e.name,document.getElementById("f-weight").value=e.weight,document.getElementById("f-desc").value=e.description||"",tempLevels=e.levels.map(t=>({...t})))}else document.getElementById("f-name").value="",document.getElementById("f-weight").value=20,document.getElementById("f-desc").value="",tempLevels=[{name:"Level 1",points:10,description:""},{name:"Level 2",points:25,description:""},{name:"Level 3",points:50,description:""},{name:"Level 4",points:80,description:""}];renderLevelsEditor(),openModal("modal-factor")}function renderLevelsEditor(){const t=document.getElementById("factor-levels-editor");0!==tempLevels.length?t.innerHTML=tempLevels.map((t,e)=>`\n    <div style="display:grid;grid-template-columns:1fr 100px 1fr auto;gap:10px;align-items:start;margin-bottom:10px;padding:10px;background:var(--bg-raised);border-radius:var(--radius-sm);border:1px solid var(--border-dim)">\n      <div class="form-group" style="gap:4px">\n        <label>Level Name</label>\n        <input type="text" value="${escHtml(t.name)}" oninput="tempLevels[${e}].name=this.value" placeholder="Level ${e+1}">\n      </div>\n      <div class="form-group" style="gap:4px">\n        <label>Points</label>\n        <input type="number" value="${t.points}" oninput="tempLevels[${e}].points=+this.value" min="0">\n      </div>\n      <div class="form-group" style="gap:4px">\n        <label>Description</label>\n        <input type="text" value="${escHtml(t.description||"")}" oninput="tempLevels[${e}].description=this.value" placeholder="Optional description">\n      </div>\n      <button class="btn btn-danger btn-icon btn-sm" style="margin-top:20px" onclick="removeTempLevel(${e})">✕</button>\n    </div>\n  `).join(""):t.innerHTML='<div class="text-dim text-sm" style="padding:12px 0">No levels yet. Click "Add Level".</div>'}function addFactorLevel(){const t=tempLevels.length>0?tempLevels[tempLevels.length-1].points:0;tempLevels.push({name:`Level ${tempLevels.length+1}`,points:t+20,description:""}),renderLevelsEditor()}function removeTempLevel(t){tempLevels.splice(t,1),renderLevelsEditor()}function saveFactor(){const t=document.getElementById("f-name").value.trim(),e=parseFloat(document.getElementById("f-weight").value)||1,a=document.getElementById("f-desc").value.trim();if(t)if(0!==tempLevels.length){if(editingFactorId){const n=state.factors.find(t=>t.id===editingFactorId);n&&(n.name=t,n.weight=e,n.description=a,n.levels=tempLevels.map(t=>({...t})))}else state.factors.push({id:uid(),name:t,weight:e,description:a,levels:tempLevels.map(t=>({...t}))});markUnsaved(),closeModal("modal-factor"),renderFactors(),toast(editingFactorId?"Factor updated":"Factor added","success")}else toast("Add at least one level","error");else toast("Factor name is required","error")}function deleteFactor(t){confirm("Delete this factor? This will remove its scores from all evaluations.")&&(state.factors=state.factors.filter(e=>e.id!==t),Object.values(state.evaluations).forEach(e=>{delete e[t]}),markUnsaved(),renderFactors(),toast("Factor deleted","info"))}function renderFactors(){const t=document.getElementById("factor-list-container");if(0===state.factors.length)return void(t.innerHTML='<div class="empty-state">\n      <svg class="empty-state-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>\n      <div class="empty-state-title">No Factors Yet</div>\n      <div class="empty-state-desc">Add compensable factors to begin building your evaluation framework.</div>\n    </div>');const e=state.factors.reduce((t,e)=>t+e.weight,0);t.innerHTML=`\n    <div style="background:var(--bg-surface);border:1px solid var(--border-dim);border-radius:var(--radius-md);padding:12px 16px;margin-bottom:14px;display:flex;align-items:center;justify-content:space-between">\n      <span class="text-dim text-mono text-sm">Total Factor Weight</span>\n      <span style="font-family:var(--font-mono);font-size:14px;color:${100===e?"var(--accent-green)":"var(--accent-gold)"};font-weight:600">${e.toFixed(1)}</span>\n    </div>\n    <div class="factor-list">\n      ${state.factors.map((t,a)=>{const n=COLORS[a%COLORS.length];return`\n        <div class="factor-card" id="fc-${t.id}">\n          <div class="factor-card-header" onclick="toggleFactorCard('${t.id}')">\n            <div class="factor-color-dot" style="background:${n}"></div>\n            <span class="factor-name">${escHtml(t.name)}</span>\n            <span class="factor-weight-badge">×${t.weight}</span>\n            <div class="inline-actions" onclick="event.stopPropagation()">\n              <button class="btn btn-ghost btn-icon btn-sm" onclick="openAddFactorModal('${t.id}')">✎</button>\n              <button class="btn btn-danger btn-icon btn-sm" onclick="deleteFactor('${t.id}')">✕</button>\n            </div>\n            <svg class="factor-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"/></svg>\n          </div>\n          <div class="factor-card-body">\n            ${t.description?`<p style="font-size:12px;color:var(--text-dim);margin:10px 0 4px">${escHtml(t.description)}</p>`:""}\n            <table class="level-table">\n              <thead><tr><th>Level</th><th>Points</th><th>Description</th></tr></thead>\n              <tbody>\n                ${t.levels.map(t=>`\n                  <tr>\n                    <td><span class="badge badge-blue">${escHtml(t.name)}</span></td>\n                    <td style="color:var(--accent-cyan);font-family:var(--font-mono)">${t.points}</td>\n                    <td>${escHtml(t.description||"—")}</td>\n                  </tr>\n                `).join("")}\n              </tbody>\n            </table>\n            <div style="margin-top:10px">\n              <div class="progress-bar">\n                <div class="progress-fill" style="width:${Math.min(100,t.weight/e*100)}%"></div>\n              </div>\n              <span style="font-size:10px;color:var(--text-dim);font-family:var(--font-mono)">${(t.weight/e*100).toFixed(1)}% of total weight</span>\n            </div>\n          </div>\n        </div>`}).join("")}\n    </div>`}function toggleFactorCard(t){document.getElementById("fc-"+t).classList.toggle("expanded")}function recalcAllWeights(){const t=state.factors.reduce((t,e)=>t+e.weight,0);0!==t&&(state.factors.forEach(e=>{e.weight=Math.round(e.weight/t*100)}),markUnsaved(),renderFactors(),toast("Weights normalized to 100","success"))}function openAddPositionModal(t=null){if(editingPositionId=t,document.getElementById("modal-position-title").textContent=t?"Edit Position":"Add Position",t){const e=state.positions.find(e=>e.id===t);e&&(document.getElementById("p-title").value=e.title,document.getElementById("p-dept").value=e.department||"",document.getElementById("p-reports").value=e.reportsTo||"",document.getElementById("p-salary").value=e.salary||"",document.getElementById("p-desc").value=e.description||"")}else document.getElementById("p-title").value="",document.getElementById("p-dept").value="",document.getElementById("p-reports").value="",document.getElementById("p-salary").value="",document.getElementById("p-desc").value="";openModal("modal-position")}function savePosition(){const t=document.getElementById("p-title").value.trim();if(!t)return void toast("Position title is required","error");const e={title:t,department:document.getElementById("p-dept").value.trim(),reportsTo:document.getElementById("p-reports").value.trim(),salary:parseFloat(document.getElementById("p-salary").value)||null,description:document.getElementById("p-desc").value.trim()};if(editingPositionId){const t=state.positions.find(t=>t.id===editingPositionId);t&&Object.assign(t,e)}else state.positions.push({id:uid(),...e});markUnsaved(),closeModal("modal-position"),renderPositionsTable(),toast(editingPositionId?"Position updated":"Position added","success")}function deletePosition(t){confirm("Delete this position?")&&(state.positions=state.positions.filter(e=>e.id!==t),delete state.evaluations[t],markUnsaved(),renderPositionsTable(),toast("Position deleted","info"))}function sortPositions(t){positionSortKey===t?positionSortAsc=!positionSortAsc:(positionSortKey=t,positionSortAsc=!0),renderPositionsTable()}function renderPositionsTable(){const t=(document.getElementById("position-search")?.value||"").toLowerCase();let e=state.positions.filter(e=>e.title.toLowerCase().includes(t)||(e.department||"").toLowerCase().includes(t));e.sort((t,e)=>{let a=t[positionSortKey]||"",n=e[positionSortKey]||"";return"salary"===positionSortKey&&(a=a||0,n=n||0),positionSortAsc?a>n?1:-1:a<n?1:-1});const a=document.getElementById("positions-tbody"),n=document.getElementById("positions-empty");if(document.getElementById("positions-count").textContent=`${e.length} position${1!==e.length?"s":""}`,0===e.length)return a.innerHTML="",void(n.style.display="block");n.style.display="none",a.innerHTML=e.map(t=>{const e=getPositionScore(t.id),a=getAssignedLevel(e);getAssignedGrade(a);return`<tr>\n      <td>${escHtml(t.title)}</td>\n      <td>${escHtml(t.department||"—")}</td>\n      <td style="color:var(--text-dim)">${escHtml(t.reportsTo||"—")}</td>\n      <td style="font-family:var(--font-mono);color:var(--accent-cyan)">${e>0?e.toFixed(0):"—"}</td>\n      <td>${a?`<span class="badge badge-blue">${escHtml(a.name)}</span>`:'<span class="text-dim">—</span>'}</td>\n      <td>\n        <div class="inline-actions">\n          <button class="btn btn-secondary btn-sm" onclick="selectEvalPosition('${t.id}');navigate('workspace')">Evaluate</button>\n          <button class="btn btn-ghost btn-icon btn-sm" onclick="openAddPositionModal('${t.id}')">✎</button>\n          <button class="btn btn-danger btn-icon btn-sm" onclick="deletePosition('${t.id}')">✕</button>\n        </div>\n      </td>\n    </tr>`}).join("")}function selectEvalPosition(t){currentEvalPositionId=t,renderWorkspace(),document.querySelectorAll(".eval-pos-item").forEach(t=>t.classList.remove("active"));const e=document.getElementById("eval-pos-"+t);e&&e.classList.add("active")}function renderWorkspace(){renderWorkspacePositionList(),currentEvalPositionId&&renderEvalArea()}let wsStatusFilter="all";function setWsFilter(t){wsStatusFilter=t,document.querySelectorAll(".ws-filter-btn").forEach(t=>t.classList.remove("active"));const e=document.getElementById("ws-filter-"+t);e&&e.classList.add("active"),renderWorkspacePositionList()}function renderWorkspacePositionList(){const t=document.getElementById("workspace-position-list");if(0===state.positions.length)return void(t.innerHTML='<div class="text-dim text-sm" style="padding:12px">No positions yet. Add them in Position Library.</div>');const e=(document.getElementById("ws-pos-search")?.value||"").toLowerCase(),a=state.positions.filter(t=>{const a=!e||t.title.toLowerCase().includes(e)||(t.department||"").toLowerCase().includes(e),n=state.evaluations[t.id]||{},s=state.factors.length,o=Object.keys(n).filter(t=>void 0!==n[t]&&n[t]>=0).length,r=s>0&&o>=s;return a&&("all"===wsStatusFilter||"done"===wsStatusFilter&&r||"wip"===wsStatusFilter&&(o>0&&!r)||"new"===wsStatusFilter&&0===o)});0!==a.length?t.innerHTML=a.map(t=>{const e=getPositionScore(t.id),a=currentEvalPositionId===t.id,n=state.evaluations[t.id]||{},s=state.factors.length,o=Object.keys(n).filter(t=>void 0!==n[t]&&n[t]>=0).length,r=s>0&&o>=s,i=r?'<span class="badge badge-green" style="font-size:9px;padding:1px 6px">Done</span>':o>0&&!r?`<span class="badge badge-gold" style="font-size:9px;padding:1px 6px">WIP ${o}/${s}</span>`:'<span style="font-size:10px;color:var(--text-dim)">Not started</span>';return`<div id="eval-pos-${t.id}" class="eval-pos-item${a?" active":""}"\n      style="padding:10px 12px;border-radius:var(--radius-sm);cursor:pointer;margin-bottom:4px;border:1px solid ${a?"var(--accent-blue)":"var(--border-dim)"};background:${a?"rgba(79,142,247,0.08)":"var(--bg-raised)"};transition:var(--transition)"\n      onclick="selectEvalPosition('${t.id}')">\n      <div style="font-size:13px;font-weight:600;color:${a?"var(--accent-blue)":"var(--text-primary)"}">${escHtml(t.title)}</div>\n      <div style="font-size:11px;color:var(--text-dim);margin-top:2px">${escHtml(t.department||"No department")}</div>\n      <div style="display:flex;align-items:center;justify-content:space-between;margin-top:6px">\n        ${r?`<span style="font-family:var(--font-mono);font-size:11px;color:var(--accent-cyan)">${e.toFixed(0)} pts</span>`:"<span></span>"}\n        ${i}\n      </div>\n    </div>`}).join(""):t.innerHTML='<div class="text-dim text-sm" style="padding:12px;text-align:center">No positions match this filter.</div>'}function renderEvalArea(){const t=state.positions.find(t=>t.id===currentEvalPositionId);if(!t)return;const e=document.getElementById("workspace-eval-area");if(0===state.factors.length)return void(e.innerHTML='<div class="empty-state"><div class="empty-state-title">No Factors Defined</div><div class="empty-state-desc">Build factors in the Factor Builder first.</div></div>');const a=state.evaluations[t.id]||{};let n=0;const s=state.factors.map((t,e)=>{const s=void 0!==a[t.id]?a[t.id]:-1,o=s>=0?(t.levels[s]?.points||0)*t.weight:0;return n+=o,{f:t,selLevel:s,pts:o,fi:e}}),o=state.factors.reduce((t,e)=>t+(e.levels.length>0?Math.max(...e.levels.map(t=>t.points)):0)*e.weight,0);e.innerHTML=`\n    <div class="card mb-4">\n      <div class="card-header">\n        <div>\n          <div style="font-family:var(--font-display);font-weight:700">${escHtml(t.title)}</div>\n          <div style="font-size:11px;color:var(--text-dim)">${escHtml(t.department||"")} ${t.reportsTo?"· Reports to "+t.reportsTo:""}</div>\n        </div>\n        <button class="btn btn-primary btn-sm" onclick="saveEvaluation()">Save Evaluation</button>\n      </div>\n      <div class="card-body">\n        ${s.map(({f:t,selLevel:e,pts:a,fi:n})=>`\n          <div class="eval-factor-row">\n            <div>\n              <div class="eval-factor-name">\n                <span style="display:inline-block;width:8px;height:8px;border-radius:50%;background:${COLORS[n%COLORS.length]};margin-right:8px"></span>\n                ${escHtml(t.name)}\n              </div>\n              <div class="eval-factor-weight">Weight: ×${t.weight}</div>\n            </div>\n            <select class="eval-level-select" id="eval-sel-${t.id}" onchange="updateEvalScore('${t.id}', this.value)">\n              <option value="-1">— Select Level —</option>\n              ${t.levels.map((t,a)=>`<option value="${a}" ${e===a?"selected":""}>${escHtml(t.name)} (${t.points} pts)</option>`).join("")}\n            </select>\n            <div class="eval-score-display" id="eval-score-${t.id}">${a>0?a.toFixed(0):"—"}</div>\n          </div>\n        `).join("")}\n        <div class="score-summary">\n          <div>\n            <div class="score-summary-label">Total Job Score</div>\n            <div class="score-summary-value" id="eval-total-score">${n.toFixed(0)}</div>\n          </div>\n          <div style="text-align:right">\n            <div class="score-summary-label">Max Possible</div>\n            <div style="font-family:var(--font-mono);font-size:14px;color:var(--text-dim)">${o.toFixed(0)}</div>\n            <div style="font-size:11px;color:var(--text-dim);margin-top:4px">${o>0?(n/o*100).toFixed(1)+"% of max":""}</div>\n          </div>\n        </div>\n      </div>\n    </div>`;const r=document.getElementById("workspace-summary-area");r&&(r.style.display=""),updateEvalSummary(),renderFactorContrib()}function updateEvalScore(t,e){if(!currentEvalPositionId)return;state.evaluations[currentEvalPositionId]||(state.evaluations[currentEvalPositionId]={});const a=parseInt(e);-1===a?delete state.evaluations[currentEvalPositionId][t]:state.evaluations[currentEvalPositionId][t]=a,invalidateScoreCache(currentEvalPositionId);const n=state.factors.find(e=>e.id===t),s=a>=0?(n.levels[a]?.points||0)*n.weight:0,o=document.getElementById("eval-score-"+t);o&&(o.textContent=s>0?s.toFixed(0):"—");const r=getPositionScore(currentEvalPositionId),i=document.getElementById("eval-total-score");i&&(i.textContent=r.toFixed(0)),updateEvalSummary(),markUnsaved()}function saveEvaluation(){markUnsaved(),renderWorkspacePositionList(),toast("Evaluation saved","success")}let _scoreCache={};function invalidateScoreCache(t){t?delete _scoreCache[t]:_scoreCache={}}function getPositionScore(t){if(void 0!==_scoreCache[t])return _scoreCache[t];const e=state.evaluations[t];if(!e)return _scoreCache[t]=0,0;const a=state.factors.reduce((t,a)=>{const n=e[a.id];return void 0===n?t:t+(a.levels[n]?.points||0)*a.weight},0);return _scoreCache[t]=a,a}let rankSortField="totalScore",rankSortAsc=!1;function rankSort(t){rankSortField===t?rankSortAsc=!rankSortAsc:(rankSortField=t,rankSortAsc="totalScore"!==t),renderRankingTable()}function renderRankingTable(){const t=(document.getElementById("ranking-search")?.value||"").toLowerCase(),e=document.getElementById("ranking-dept-filter")?.value||"",a=[...new Set(state.positions.map(t=>t.department).filter(Boolean))],n=document.getElementById("ranking-dept-filter");if(n){const t=n.value;n.innerHTML='<option value="">All Departments</option>'+a.map(e=>`<option value="${escHtml(e)}" ${e===t?"selected":""}>${escHtml(e)}</option>`).join("")}let s=state.positions.filter(t=>state.evaluations[t.id]&&Object.keys(state.evaluations[t.id]).length>0).map(t=>({...t,totalScore:getPositionScore(t.id)})).filter(a=>{const n=(a.title+a.department).toLowerCase().includes(t),s=!e||a.department===e;return n&&s});s.sort((t,e)=>{let a=t[rankSortField]||"",n=e[rankSortField]||"";return"totalScore"===rankSortField&&(a=a||0,n=n||0),rankSortAsc?a>n?1:-1:a<n?1:-1});const o=s.length>0?Math.max(...s.map(t=>t.totalScore)):1,r=document.getElementById("ranking-tbody"),i=document.getElementById("ranking-empty");if(document.getElementById("ranking-count").textContent=`${s.length} evaluated`,0===s.length)return r.innerHTML="",void(i.style.display="block");i.style.display="none",r.innerHTML=s.map((t,e)=>{const a=getAssignedLevel(t.totalScore),n=getAssignedGrade(a),s=o>0?t.totalScore/o*100:0;return`<tr>\n      <td class="rank-number">${e+1}</td>\n      <td>${escHtml(t.title)}</td>\n      <td>${t.department?`<span class="badge badge-cyan">${escHtml(t.department)}</span>`:"—"}</td>\n      <td class="score-bar-cell">\n        <div class="score-bar-wrap">\n          <div class="score-bar"><div class="score-bar-fill" style="width:${s}%"></div></div>\n        </div>\n      </td>\n      <td class="score-val">${t.totalScore.toFixed(0)}</td>\n      <td>${a?`<span class="badge badge-purple">${escHtml(a.name)}</span>`:"—"}</td>\n      <td>${n?`<span class="badge badge-gold">${escHtml(n.label)}</span>`:"—"}</td>\n    </tr>`}).join("")}function renderClustering(){renderClusterBands(),renderClusterAssignments(),renderClusterDistChart()}function renderClusterBands(){const t=document.getElementById("cluster-bands-list");0!==state.clusters.length?t.innerHTML=state.clusters.map((t,e)=>`\n    <div style="display:grid;grid-template-columns:auto 1fr 1fr auto;gap:8px;align-items:center;margin-bottom:8px;padding:10px;background:var(--bg-raised);border:1px solid var(--border-dim);border-radius:var(--radius-sm)">\n      <input type="text" value="${escHtml(t.name)}" style="width:80px" oninput="state.clusters[${e}].name=this.value;renderClusterAssignments();markUnsaved()">\n      <input type="number" value="${t.min}" placeholder="Min" oninput="state.clusters[${e}].min=+this.value;renderClusterAssignments();markUnsaved()">\n      <input type="number" value="${t.max}" placeholder="Max" oninput="state.clusters[${e}].max=+this.value;renderClusterAssignments();markUnsaved()">\n      <button class="btn btn-danger btn-icon btn-sm" onclick="removeCluster(${e})">✕</button>\n    </div>\n  `).join(""):t.innerHTML='<div class="text-dim text-sm">No levels defined. Add levels or auto-generate.</div>'}function addClusterBand(){const t=state.clusters.length>0?state.clusters[state.clusters.length-1].max:0;state.clusters.push({name:`Level ${state.clusters.length+1}`,min:t+1,max:t+150}),markUnsaved(),renderClustering()}function removeCluster(t){state.clusters.splice(t,1),markUnsaved(),renderClustering()}function autoGenerateClusters(){const t=state.positions.map(t=>getPositionScore(t.id)).filter(t=>t>0);if(0===t.length)return void toast("No evaluated positions to cluster","error");const e=Math.min(...t),a=Math.max(...t),n=(a-e)/5;state.clusters=Array.from({length:5},(t,s)=>({name:`Level ${s+1}`,min:Math.round(e+s*n),max:4===s?Math.round(a)+1:Math.round(e+(s+1)*n)})),markUnsaved(),renderClustering(),toast("Auto-generated 5 levels","success")}function renderClusterAssignments(){const t=document.getElementById("cluster-assignments-tbody"),e=state.positions.map(t=>({...t,score:getPositionScore(t.id)})).filter(t=>t.score>0).sort((t,e)=>e.score-t.score);0!==e.length?t.innerHTML=e.map(t=>{const e=getAssignedLevel(t.score);return`<tr>\n      <td>${escHtml(t.title)}</td>\n      <td style="font-family:var(--font-mono);color:var(--accent-cyan)">${t.score.toFixed(0)}</td>\n      <td>${e?`<span class="badge badge-purple">${escHtml(e.name)}</span>`:'<span class="text-dim">—</span>'}</td>\n      <td>\n        <select style="font-size:12px;padding:4px 8px;background:var(--bg-raised);border:1px solid var(--border-mid);border-radius:4px;color:var(--text-primary)"\n          onchange="setManualLevel('${t.id}', this.value)">\n          <option value="">Auto</option>\n          ${state.clusters.map(e=>`<option value="${e.name}" ${(state.positions.find(e=>e.id===t.id)||{}).manualLevel===e.name?"selected":""}>${escHtml(e.name)}</option>`).join("")}\n        </select>\n      </td>\n    </tr>`}).join(""):t.innerHTML='<tr><td colspan="4" class="text-dim text-sm" style="padding:20px;text-align:center">No evaluated positions</td></tr>'}function setManualLevel(t,e){const a=state.positions.find(e=>e.id===t);a&&(a.manualLevel=e||null,markUnsaved())}function getAssignedLevel(t){if(!t||0===state.clusters.length)return null;const e=state.positions.find(e=>getPositionScore(e.id)===t);return e&&e.manualLevel?state.clusters.find(t=>t.name===e.manualLevel):state.clusters.find(e=>t>=e.min&&t<=e.max)||null}let salaryMatrix=[],matrixConfig={numGrades:5,numSteps:8,stepInterval:6,movementRate:15,grade1Min:3e4},salarySalaryCharts={};function switchSalaryTab(t){["matrix","assignments"].forEach(e=>{document.getElementById("salary-tab-"+e)?.classList.toggle("active",e===t);const a=document.getElementById("salary-pane-"+e);a&&(a.style.display=e===t?"":"none")}),"assignments"===t&&(renderSalaryGrades(),renderSalaryAssignments()),"matrix"===t&&renderSalaryMatrix()}function roundUp100(t){return 100*Math.ceil(t/100)}function computeSalaryMatrix(t){const{numGrades:e,numSteps:a,stepInterval:n,movementRate:s,grade1Min:o}=t,r=n/100,i=s/100,l=[];let d=o;for(let t=0;t<e;t++){const n=[];n.push(d);for(let t=1;t<a;t++)n.push(n[t-1]*(1+r));if(l.push(n),t<e-1){const t=n[n.length-2];d=roundUp100(t+(n[n.length-1]-t)*i)}}return l}function onMatrixConfigChange(){const t=Math.max(5,parseInt(document.getElementById("sm-num-grades").value)||5),e=Math.max(5,parseInt(document.getElementById("sm-num-steps").value)||5),a=parseFloat(document.getElementById("sm-step-interval").value)||6,n=parseFloat(document.getElementById("sm-movement-rate").value)||15,s=parseFloat(document.getElementById("sm-grade1-min").value)||3e4;document.getElementById("sm-num-grades").value=t,document.getElementById("sm-num-steps").value=e,matrixConfig={numGrades:t,numSteps:e,stepInterval:a,movementRate:n,grade1Min:s},salaryMatrix=[],state.matrixConfig=matrixConfig,markUnsaved(),renderSalaryMatrix()}function renderSalary(){if(state.matrixConfig){matrixConfig={...matrixConfig,...state.matrixConfig};const t=matrixConfig,e=t=>document.getElementById(t);e("sm-num-grades")&&(e("sm-num-grades").value=t.numGrades),e("sm-num-steps")&&(e("sm-num-steps").value=t.numSteps),e("sm-step-interval")&&(e("sm-step-interval").value=t.stepInterval),e("sm-movement-rate")&&(e("sm-movement-rate").value=t.movementRate),e("sm-grade1-min")&&(e("sm-grade1-min").value=t.grade1Min)}renderSalaryMatrix(),renderSalaryGrades(),renderSalaryAssignments()}function renderSalaryMatrix(){const t=matrixConfig.numGrades,e=matrixConfig.numSteps;(salaryMatrix.length!==t||salaryMatrix[0]&&salaryMatrix[0].length!==e)&&(salaryMatrix=computeSalaryMatrix(matrixConfig));const{numGrades:a,numSteps:n,stepInterval:s,movementRate:o}=matrixConfig,r=salaryMatrix[0][0],i=salaryMatrix[a-1][n-1],l=(((salaryMatrix[0][n-1]-salaryMatrix[0][0])/salaryMatrix[0][0]*100).toFixed(1),((salaryMatrix[a-1][n-1]-salaryMatrix[a-1][0])/salaryMatrix[a-1][0]*100).toFixed(1),document.getElementById("salary-matrix-wrap")),d=Array.from({length:n},(t,e)=>`<th class="${0===e?"step-first":e===n-1?"step-last":""}">${0===e?"Step 1 (Min)":e===n-1?`Step ${n} (Max)`:`Step ${e+1}`}</th>`).join(""),c=salaryMatrix.map((t,e)=>{const a=e+1,s=Math.floor((n-1)/2);return`<tr>\n      <td class="grade-label">\n        <span class="badge badge-gold" style="margin-right:6px;font-size:10px">G${a}</span>\n        Grade ${a}\n      </td>\n      ${t.map((t,a)=>{let o="editable";return 0===a?o+=" step-min":a===n-1?o+=" step-max":a===s&&(o+=" step-mid"),`<td class="${o}" ondblclick="editMatrixCell(this,${e},${a})">\n        <span class="cell-val">${fmtCurrency(t)}</span>\n      </td>`}).join("")}\n      <td style="font-family:var(--font-mono);font-size:10px;color:var(--text-dim);text-align:center">${((t[t.length-1]-t[0])/t[0]*100).toFixed(1)}%</td>\n    </tr>`}).join("");l.innerHTML=`\n    <div style="padding:12px 16px;border-bottom:1px solid var(--border-dim);display:flex;gap:20px;flex-wrap:wrap">\n      <div class="matrix-stat"><div class="matrix-stat-val">${fmtCurrency(r)}</div><div class="matrix-stat-lbl">Overall Min</div></div>\n      <div class="matrix-stat"><div class="matrix-stat-val">${fmtCurrency(i)}</div><div class="matrix-stat-lbl">Overall Max</div></div>\n      <div class="matrix-stat"><div class="matrix-stat-val">${a}</div><div class="matrix-stat-lbl">Grades</div></div>\n      <div class="matrix-stat"><div class="matrix-stat-val">${n}</div><div class="matrix-stat-lbl">Steps</div></div>\n      <div class="matrix-stat"><div class="matrix-stat-val">${s}%</div><div class="matrix-stat-lbl">Step Interval</div></div>\n      <div class="matrix-stat"><div class="matrix-stat-val">${o}%</div><div class="matrix-stat-lbl">Grade Movement</div></div>\n    </div>\n    <div style="overflow-x:auto">\n      <table class="salary-matrix-table">\n        <thead>\n          <tr>\n            <th class="grade-col">Grade</th>\n            ${d}\n            <th>Range Spread</th>\n          </tr>\n        </thead>\n        <tbody>${c}</tbody>\n      </table>\n    </div>\n    <div style="padding:10px 16px;border-top:1px solid var(--border-dim);font-size:11px;color:var(--text-dim);font-family:var(--font-mono)">\n      <span style="color:var(--accent-green)">■</span> Min &nbsp;\n      <span style="color:var(--accent-gold)">■</span> Mid &nbsp;\n      <span style="color:var(--accent-red)">■</span> Max &nbsp;·&nbsp;\n      Double-click any cell to edit its value manually\n    </div>`,setTimeout(renderSalaryCharts,50),document.getElementById("matrix-status-badge").textContent=`${a} grades × ${n} steps`}function editMatrixCell(t,e,a){const n=salaryMatrix[e][a],s=t.querySelector(".cell-val");if(t.querySelector("input"))return;const o=document.createElement("input");o.type="number",o.value=n,o.style.cssText="background:transparent;border:none;outline:none;width:80px;font-family:var(--font-mono);font-size:11px;color:inherit;text-align:right;padding:0",s.style.display="none",t.appendChild(o),o.focus(),o.select();const r=()=>{const t=parseFloat(o.value)||n;salaryMatrix[e][a]=t,s.textContent=fmtCurrency(t),s.style.display="",o.remove(),markUnsaved(),setTimeout(renderSalaryCharts,50)};o.addEventListener("blur",r),o.addEventListener("keydown",t=>{"Enter"===t.key&&r(),"Escape"===t.key&&(s.style.display="",o.remove())})}function fmtCurrency(t){const e=Math.round(100*t)/100,a=e===Math.floor(e);return currencySymbol+(a?Math.round(e).toLocaleString():e.toLocaleString(void 0,{minimumFractionDigits:2,maximumFractionDigits:2}))}function destroySalaryCharts(){Object.values(salarySalaryCharts).forEach(t=>{try{t.destroy()}catch(t){}}),salarySalaryCharts={}}function renderSalaryCharts(){destroySalaryCharts(),0!==salaryMatrix.length&&(renderSalaryGradesChart(),renderSalaryStepsChart(),renderGradeOverlapChart())}function renderSalaryGradesChart(){const t=document.getElementById("chart-salary-grades");if(!t)return;const e=getChartColors(),a=salaryMatrix.map((t,e)=>`Grade ${e+1}`),n=salaryMatrix[0].length,s=Math.floor((n-1)/2),o=salaryMatrix.map(t=>t[0]),r=salaryMatrix.map(t=>t[s]),i=salaryMatrix.map(t=>t[n-1]);salarySalaryCharts.grades=new Chart(t,{type:"line",data:{labels:a,datasets:[{label:"Step 1 (Min)",data:o,borderColor:"#2ecc8a",backgroundColor:"rgba(46,204,138,0.08)",pointBackgroundColor:"#2ecc8a",tension:.3,fill:!1,borderWidth:2},{label:`Step ${s+1} (Mid)`,data:r,borderColor:"#f5c842",backgroundColor:"rgba(245,200,66,0.08)",pointBackgroundColor:"#f5c842",tension:.3,fill:!1,borderWidth:2,borderDash:[4,3]},{label:`Step ${n} (Max)`,data:i,borderColor:"#ff4f6b",backgroundColor:"rgba(255,79,107,0.08)",pointBackgroundColor:"#ff4f6b",tension:.3,fill:!1,borderWidth:2}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{labels:{color:e.legend,font:{family:"'DM Mono'",size:9},boxWidth:12}}},scales:{x:{ticks:{color:e.tick,font:{family:"'DM Mono'",size:9}},grid:{color:e.grid}},y:{ticks:{color:e.tick,font:{family:"'DM Mono'",size:9},callback:t=>fmtCurrency(t)},grid:{color:e.grid}}}}})}function renderSalaryStepsChart(){const t=document.getElementById("chart-salary-steps");if(!t)return;const e=getChartColors(),a=salaryMatrix[0].length,n=Array.from({length:a},(t,e)=>`Step ${e+1}`),s=salaryMatrix.map((t,e)=>({label:`Grade ${e+1}`,data:t,borderColor:COLORS[e%COLORS.length],backgroundColor:"transparent",pointBackgroundColor:COLORS[e%COLORS.length],tension:.3,borderWidth:1.5,pointRadius:3}));salarySalaryCharts.steps=new Chart(t,{type:"line",data:{labels:n,datasets:s},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{labels:{color:e.legend,font:{family:"'DM Mono'",size:9},boxWidth:12}}},scales:{x:{ticks:{color:e.tick,font:{family:"'DM Mono'",size:9}},grid:{color:e.grid}},y:{ticks:{color:e.tick,font:{family:"'DM Mono'",size:9},callback:t=>fmtCurrency(t)},grid:{color:e.grid}}}}})}function renderGradeOverlapChart(){const t=document.getElementById("chart-grade-overlap");if(!t)return;const e=getChartColors(),a=salaryMatrix[0].length,n=(salaryMatrix.map((t,e)=>`Grade ${e+1}`),salaryMatrix.map((t,e)=>({label:`Grade ${e+1}`,data:[{x:e,y:t[0],y2:t[a-1]}],backgroundColor:COLORS[e%COLORS.length]+"aa",borderColor:COLORS[e%COLORS.length],borderWidth:1.5,borderSkipped:!1})),salaryMatrix.map((t,e)=>({grade:`Grade ${e+1}`,min:t[0],max:t[a-1],mid:t[Math.floor((a-1)/2)]})));salarySalaryCharts.overlap=new Chart(t,{type:"bar",data:{labels:n.map(t=>t.grade),datasets:[{label:"Min Salary",data:n.map(t=>t.min),backgroundColor:"rgba(46,204,138,0.5)",borderColor:"#2ecc8a",borderWidth:1,stack:"stack0"},{label:"Range (Min→Max)",data:n.map(t=>t.max-t.min),backgroundColor:n.map((t,e)=>COLORS[e%COLORS.length]+"55"),borderColor:n.map((t,e)=>COLORS[e%COLORS.length]),borderWidth:1.5,stack:"stack0"}]},options:{responsive:!0,maintainAspectRatio:!1,indexAxis:"y",plugins:{legend:{labels:{color:e.legend,font:{family:"'DM Mono'",size:9},boxWidth:12}},tooltip:{callbacks:{label:t=>{const e=n[t.dataIndex];return 0===t.datasetIndex?`Min: $${e.min.toLocaleString()}`:`Max: $${e.max.toLocaleString()} · Range: $${(e.max-e.min).toLocaleString()}`}}}},scales:{x:{stacked:!0,ticks:{color:e.tick,font:{family:"'DM Mono'",size:9},callback:t=>fmtCurrency(t)},grid:{color:e.grid}},y:{stacked:!0,ticks:{color:e.tick,font:{family:"'DM Mono'",size:9}},grid:{color:e.grid}}}}})}function exportMatrixCSV(){if(0===salaryMatrix.length)return void toast("Generate matrix first","error");const t=salaryMatrix[0].length,e=[["Grade",...Array.from({length:t},(e,a)=>0===a?"Step 1 (Min)":a===t-1?`Step ${t} (Max)`:`Step ${a+1}`),"Range Spread %"]];salaryMatrix.forEach((t,a)=>{const n=((t[t.length-1]-t[0])/t[0]*100).toFixed(1);e.push([`Grade ${a+1}`,...t,n+"%"])}),downloadCSV(e,"salary-matrix.csv"),toast("Exported salary matrix CSV","success")}function exportMatrixXLSX(){if("undefined"==typeof XLSX)return void toast("XLSX library not loaded","error");if(0===salaryMatrix.length)return void toast("Generate matrix first","error");const t=XLSX.utils.book_new(),e=salaryMatrix[0].length,a=[["Grade",...Array.from({length:e},(t,a)=>0===a?"Step 1 (Min)":a===e-1?`Step ${e} (Max)`:`Step ${a+1}`),"Range Spread %"]];salaryMatrix.forEach((t,e)=>{const n=parseFloat(((t[t.length-1]-t[0])/t[0]*100).toFixed(1));a.push([`Grade ${e+1}`,...t,n])});const n=XLSX.utils.aoa_to_sheet(a);n["!cols"]=[{wch:10},...Array(e).fill({wch:14}),{wch:14}],XLSX.utils.book_append_sheet(t,n,"Salary Matrix");const s=[["Parameter","Value"],["Number of Grades",matrixConfig.numGrades],["Steps per Grade",matrixConfig.numSteps],["Step Interval %",matrixConfig.stepInterval],["Grade Movement Rate %",matrixConfig.movementRate],["Grade 1 Minimum",matrixConfig.grade1Min]];XLSX.utils.book_append_sheet(t,XLSX.utils.aoa_to_sheet(s),"Configuration"),XLSX.writeFile(t,"salary-matrix.xlsx"),toast("Exported salary matrix XLSX","success")}function renderImportedMatrix(){if(0===salaryMatrix.length)return;const t=salaryMatrix.length,e=salaryMatrix[0].length;matrixConfig.numGrades=t,matrixConfig.numSteps=e;salaryMatrix=salaryMatrix,renderSalaryMatrix()}function renderSalaryGrades(){const t=document.getElementById("salary-grades-list");0!==state.salaryGrades.length?t.innerHTML=state.salaryGrades.map((t,e)=>`\n    <div style="display:grid;grid-template-columns:auto 1fr 1fr 1fr 1fr auto;gap:8px;align-items:center;margin-bottom:8px;padding:10px;background:var(--bg-raised);border:1px solid var(--border-dim);border-radius:var(--radius-sm)">\n      <span class="badge badge-gold" style="font-size:11px">${e+1}</span>\n      <input type="text" value="${escHtml(t.label)}" placeholder="Grade label" oninput="state.salaryGrades[${e}].label=this.value;renderSalaryAssignments();markUnsaved()">\n      <input type="number" value="${t.min}" placeholder="Min" oninput="state.salaryGrades[${e}].min=+this.value;renderSalaryAssignments();markUnsaved()">\n      <input type="number" value="${t.mid}" placeholder="Midpoint" oninput="state.salaryGrades[${e}].mid=+this.value;renderSalaryAssignments();markUnsaved()">\n      <input type="number" value="${t.max}" placeholder="Max" oninput="state.salaryGrades[${e}].max=+this.value;renderSalaryAssignments();markUnsaved()">\n      <button class="btn btn-danger btn-icon btn-sm" onclick="removeSalaryGrade(${e})">✕</button>\n    </div>\n  `).join(""):t.innerHTML='<div class="text-dim text-sm">No salary grades defined.</div>'}function addSalaryGrade(){const t=state.salaryGrades[state.salaryGrades.length-1],e=t?t.max+1e3:3e4;state.salaryGrades.push({label:`Grade ${state.salaryGrades.length+1}`,min:e,mid:e+15e3,max:e+3e4}),markUnsaved(),renderSalary()}function removeSalaryGrade(t){state.salaryGrades.splice(t,1),markUnsaved(),renderSalary()}function getAssignedGrade(t){if(!t||0===state.salaryGrades.length||0===state.clusters.length)return null;const e=state.clusters.findIndex(e=>e.name===t.name);return state.salaryGrades[e]||null}function renderSalaryAssignments(){const t=document.getElementById("salary-assignments-tbody"),e=state.positions.map(t=>({...t,score:getPositionScore(t.id)})).filter(t=>t.score>0).sort((t,e)=>e.score-t.score);0!==e.length?t.innerHTML=e.map(t=>{const e=getAssignedLevel(t.score),a=getAssignedGrade(e),n=a&&a.mid&&t.salary?(t.salary/a.mid*100).toFixed(1)+"%":"—",s=a&&a.mid&&t.salary?t.salary<a.min?"var(--accent-red)":t.salary>a.max?"var(--accent-gold)":"var(--accent-green)":"var(--text-dim)";return`<tr>\n      <td>${escHtml(t.title)}</td>\n      <td style="font-family:var(--font-mono);color:var(--accent-cyan)">${t.score.toFixed(0)}</td>\n      <td>${e?`<span class="badge badge-purple">${escHtml(e.name)}</span>`:"—"}</td>\n      <td>${a?`<span class="badge badge-gold">${escHtml(a.label)}</span>`:"—"}</td>\n      <td style="font-family:var(--font-mono)">${a?fmtCurrency(a.min):"—"}</td>\n      <td style="font-family:var(--font-mono)">${a?fmtCurrency(a.mid):"—"}</td>\n      <td style="font-family:var(--font-mono)">${a?fmtCurrency(a.max):"—"}</td>\n      <td style="font-family:var(--font-mono)">${t.salary?fmtCurrency(t.salary):"—"}</td>\n      <td style="font-family:var(--font-mono);color:${s}">${n}</td>\n    </tr>`}).join(""):t.innerHTML='<tr><td colspan="9" class="text-dim text-sm" style="padding:20px;text-align:center">No evaluated positions</td></tr>'}function renderAnalytics(){destroyCharts(),setTimeout(()=>{renderScoreDistChart(),renderLevelDistChart(),renderDeptScoreChart(),renderFactorWeightChart(),renderScoreSalaryChart()},50)}function destroyCharts(){Object.values(charts).forEach(t=>{try{t.destroy()}catch(t){}}),charts={},destroySalaryCharts()}const chartDefaults={color:"rgba(138,155,191,0.8)",plugins:()=>({legend:{labels:{color:getChartColors().legend,font:{family:"'DM Mono', monospace",size:10}}}}),scales:(t,e)=>{const a=getChartColors();return{x:{ticks:{color:a.tick,font:{family:"'DM Mono'",size:9}},grid:{color:a.grid},title:t?{display:!0,text:t,color:a.tick,font:{size:10}}:void 0},y:{ticks:{color:a.tick,font:{family:"'DM Mono'",size:9}},grid:{color:a.grid},title:e?{display:!0,text:e,color:a.tick,font:{size:10}}:void 0}}}};function renderScoreDistChart(){const t=state.positions.map(t=>getPositionScore(t.id)).filter(t=>t>0);if(0===t.length)return;const e=Math.min(...t),a=(Math.max(...t)-e)/8||1,n=Array(8).fill(0);t.forEach(t=>{const s=Math.min(7,Math.floor((t-e)/a));n[s]++});const s=Array.from({length:8},(t,n)=>Math.round(e+n*a)+"–"+Math.round(e+(n+1)*a)),o=document.getElementById("chart-score-dist");o&&(charts.scoreDist=new Chart(o,{type:"bar",data:{labels:s,datasets:[{data:n,backgroundColor:"rgba(79,142,247,0.5)",borderColor:"#4f8ef7",borderWidth:1}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{display:!1},...chartDefaults.plugins()},scales:chartDefaults.scales("Score Range","Count")}}))}function renderLevelDistChart(){if(0===state.clusters.length)return;const t={};state.clusters.forEach(e=>{t[e.name]=0}),state.positions.forEach(e=>{const a=getPositionScore(e.id);if(a>0){const e=getAssignedLevel(a);e&&(t[e.name]=(t[e.name]||0)+1)}});const e=Object.keys(t),a=Object.values(t),n=document.getElementById("chart-level-dist");n&&(charts.levelDist=new Chart(n,{type:"doughnut",data:{labels:e,datasets:[{data:a,backgroundColor:COLORS.slice(0,e.length).map(t=>t+"99"),borderColor:COLORS.slice(0,e.length),borderWidth:1}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{...chartDefaults.plugins(),legend:{position:"right",labels:{color:getChartColors().legend,font:{family:"'DM Mono'",size:9}}}}}}))}function renderDeptScoreChart(){const t={};state.positions.forEach(e=>{const a=getPositionScore(e.id);if(a>0){const n=e.department||"Unknown";t[n]||(t[n]=[]),t[n].push(a)}});const e=Object.keys(t),a=e.map(e=>(t[e].reduce((t,e)=>t+e,0)/t[e].length).toFixed(0)),n=document.getElementById("chart-dept-score");n&&(charts.deptScore=new Chart(n,{type:"bar",data:{labels:e,datasets:[{data:a,backgroundColor:COLORS.slice(0,e.length).map(t=>t+"88"),borderColor:COLORS.slice(0,e.length),borderWidth:1}]},options:{responsive:!0,maintainAspectRatio:!1,indexAxis:"y",plugins:{legend:{display:!1},...chartDefaults.plugins()},scales:{x:{ticks:{color:getChartColors().tick,font:{family:"'DM Mono'",size:9}},grid:{color:getChartColors().grid}},y:{ticks:{color:getChartColors().tick,font:{family:"'DM Mono'",size:9}},grid:{color:getChartColors().grid}}}}}))}function renderFactorWeightChart(){if(0===state.factors.length)return;const t=document.getElementById("chart-factor-weight");t&&(charts.factorWeight=new Chart(t,{type:"doughnut",data:{labels:state.factors.map(t=>t.name),datasets:[{data:state.factors.map(t=>t.weight),backgroundColor:COLORS.slice(0,state.factors.length).map(t=>t+"99"),borderColor:COLORS.slice(0,state.factors.length),borderWidth:1}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{...chartDefaults.plugins(),legend:{position:"right",labels:{color:getChartColors().legend,font:{family:"'DM Mono'",size:9}}}}}}))}function renderScoreSalaryChart(){const t=state.positions.filter(t=>t.salary&&getPositionScore(t.id)>0).map(t=>({x:getPositionScore(t.id),y:t.salary,label:t.title})),e=document.getElementById("chart-score-salary");e&&(charts.scoreSalary=new Chart(e,{type:"scatter",data:{datasets:[{data:t,backgroundColor:"rgba(0,212,255,0.5)",borderColor:"#00d4ff",pointRadius:5,pointHoverRadius:7}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{display:!1},...chartDefaults.plugins(),tooltip:{callbacks:{label:e=>{const a=t[e.dataIndex];return a?`${a.label}: ${a.x} pts / $${a.y?.toLocaleString()}`:""}}}},scales:chartDefaults.scales("Job Score","Salary ($)")}}))}function renderDashboard(){document.getElementById("stat-factors").textContent=state.factors.length,document.getElementById("stat-positions").textContent=state.positions.length;const t=state.positions.filter(t=>state.evaluations[t.id]&&Object.keys(state.evaluations[t.id]).length>0).length;document.getElementById("stat-evaluated").textContent=t,document.getElementById("stat-levels").textContent=state.clusters.length;const e=state.positions.map(t=>({...t,score:getPositionScore(t.id)})).filter(t=>t.score>0).sort((t,e)=>e.score-t.score).slice(0,5),a=document.getElementById("dash-top-positions");if(0===e.length)a.innerHTML='<div class="empty-state"><div class="empty-state-title">No Evaluations Yet</div></div>';else{const t=e[0].score;a.innerHTML=`<div style="padding:12px 16px">${e.map((e,a)=>`\n      <div style="display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:1px solid var(--border-dim)">\n        <span style="font-family:var(--font-mono);font-size:11px;color:var(--text-dim);min-width:20px">${a+1}</span>\n        <div style="flex:1">\n          <div style="font-size:13px;font-weight:600">${escHtml(e.title)}</div>\n          <div class="score-bar" style="margin-top:4px"><div class="score-bar-fill" style="width:${(e.score/t*100).toFixed(0)}%"></div></div>\n        </div>\n        <span style="font-family:var(--font-mono);font-size:12px;color:var(--accent-cyan)">${e.score.toFixed(0)}</span>\n      </div>\n    `).join("")}</div>`}const n=document.getElementById("dash-weight-chart");if(n){try{charts.dashWeight?.destroy()}catch(t){}state.factors.length>0&&(charts.dashWeight=new Chart(n,{type:"doughnut",data:{labels:state.factors.map(t=>t.name),datasets:[{data:state.factors.map(t=>t.weight),backgroundColor:COLORS.slice(0,state.factors.length).map(t=>t+"99"),borderColor:COLORS.slice(0,state.factors.length),borderWidth:1}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{...chartDefaults.plugins(),legend:{position:"right",labels:{color:getChartColors().legend,font:{family:"'DM Mono'",size:9},boxWidth:10}}}}}))}}let currentImportTab="positions",pendingImportPayload=null;function showImportPositions(){navigate("datamanagement"),document.getElementById("dm-import-type").value="positions",dmUpdateColHint()}function switchImportTab(t){currentImportTab=t,["positions","factors","clusters","salary","salarymatrix","project"].forEach(e=>{document.getElementById("import-tab-"+e)?.classList.toggle("active",e===t);const a=document.getElementById("import-pane-"+e);a&&(a.style.display=e===t?"":"none")}),pendingImportPayload=null,document.getElementById("import-confirm-btn").style.display="none"}function handleFileDrop(t,e){t.preventDefault();const a=t.dataTransfer.files[0];a&&processImportFile(a,e)}function handleImportFile(t,e){const a=t.target.files[0];a&&processImportFile(a,e),t.target.value=""}function processImportFile(t,e){const a=t.name.split(".").pop().toLowerCase(),n=new FileReader;if("project"===e)return n.onload=t=>{try{const e=JSON.parse(t.target.result);pendingImportPayload={type:"project",data:e},document.getElementById("import-preview-project").innerHTML=`\n          <div style="background:var(--bg-raised);border:1px solid var(--accent-green);border-radius:var(--radius-sm);padding:12px;font-size:12px;color:var(--accent-green)">\n            ✓ Valid project file: <strong>${escHtml(e.projectName||"Unnamed")}</strong><br>\n            <span style="color:var(--text-dim)">${(e.factors||[]).length} factors · ${(e.positions||[]).length} positions · ${Object.keys(e.evaluations||{}).length} evaluations</span>\n          </div>`,document.getElementById("import-confirm-btn").style.display="flex",document.getElementById("import-confirm-btn").textContent="Import Project"}catch(t){document.getElementById("import-preview-project").innerHTML='<div style="color:var(--accent-red);font-size:12px">✕ Invalid JSON file</div>'}},void n.readAsText(t);"xlsx"===a||"xls"===a?(n.onload=a=>{const n=XLSX.read(new Uint8Array(a.target.result),{type:"array"}),s=n.Sheets[n.SheetNames[0]];parseImportRows(XLSX.utils.sheet_to_json(s,{defval:""}),e,t.name)},n.readAsArrayBuffer(t)):(n.onload=a=>{parseImportRows(parseCSVToObjects(a.target.result),e,t.name)},n.readAsText(t))}function parseCSVToObjects(t){const e=t.split("\n").map(t=>t.trim()).filter(t=>t);if(e.length<2)return[];const a=splitCSVLine(e[0]).map(t=>t.replace(/^["']|["']$/g,"").trim());return e.slice(1).map(t=>{const e=splitCSVLine(t).map(t=>t.replace(/^["']|["']$/g,"").trim()),n={};return a.forEach((t,a)=>{n[t]=e[a]||""}),n}).filter(t=>Object.values(t).some(t=>t))}function splitCSVLine(t){const e=[];let a="",n=!1;for(let s=0;s<t.length;s++){const o=t[s];'"'===o?n=!n:","!==o||n?a+=o:(e.push(a),a="")}return e.push(a),e}function normalizeKey(t){return String(t).toLowerCase().replace(/[\s_\-]/g,"")}function parseImportRows(t,e,a){const n=document.getElementById("import-preview-"+e),s=document.getElementById("import-confirm-btn");if(0===t.length)return n.innerHTML='<div style="color:var(--accent-red);font-size:12px">✕ No data rows found in file</div>',void(s.style.display="none");const o=t.map(t=>{const e={};return Object.keys(t).forEach(a=>{e[normalizeKey(a)]=t[a]}),e});let r=[],i="";if("positions"===e?(r=o.map(t=>({id:uid(),title:t.title||t.positiontitle||t.name||"",department:t.department||t.dept||"",reportsTo:t.reportsto||t.manager||t.supervisor||"",salary:parseFloat(t.salary||t.currentsal||t.currentsal||"0")||null,description:t.description||t.desc||""})).filter(t=>t.title),i=`<div style="color:var(--accent-green);font-size:12px;margin-bottom:8px">✓ Found ${r.length} valid position(s) in "${escHtml(a)}"</div>\n      <div class="table-wrap" style="max-height:180px;overflow-y:auto">\n        <table><thead><tr><th>Title</th><th>Department</th><th>Reports To</th><th>Salary</th></tr></thead>\n        <tbody>${r.slice(0,8).map(t=>`<tr><td>${escHtml(t.title)}</td><td>${escHtml(t.department)}</td><td>${escHtml(t.reportsTo)}</td><td>${t.salary?fmtCurrency(t.salary):"—"}</td></tr>`).join("")}\n        ${r.length>8?`<tr><td colspan="4" style="color:var(--text-dim);font-size:11px">...and ${r.length-8} more</td></tr>`:""}</tbody></table></div>`):"factors"===e?(r=o.map(t=>{const e=[];for(let a=1;a<=8;a++){const n=t[`level${a}name`]||t[`l${a}name`]||"",s=parseFloat(t[`level${a}points`]||t[`l${a}points`]||"0")||0,o=t[`level${a}desc`]||t[`level${a}description`]||"";n&&e.push({name:n,points:s,description:o})}return{id:uid(),name:t.factorname||t.factor||t.name||"",weight:parseFloat(t.weight||"10")||10,description:t.description||t.desc||"",levels:e}}).filter(t=>t.name&&t.levels.length>0),i=`<div style="color:var(--accent-green);font-size:12px;margin-bottom:8px">✓ Found ${r.length} valid factor(s) in "${escHtml(a)}"</div>\n      <div class="table-wrap" style="max-height:180px;overflow-y:auto">\n        <table><thead><tr><th>Factor</th><th>Weight</th><th>Levels</th></tr></thead>\n        <tbody>${r.slice(0,6).map(t=>`<tr><td>${escHtml(t.name)}</td><td>${t.weight}</td><td>${t.levels.map(t=>t.name).join(", ")}</td></tr>`).join("")}\n        ${r.length>6?`<tr><td colspan="3" style="color:var(--text-dim);font-size:11px">...and ${r.length-6} more</td></tr>`:""}</tbody></table></div>`):"salary"===e?(r=o.map(t=>({label:t.label||t.grade||t.gradename||`Grade ${Math.random()}`,min:parseFloat(t.min||t.minimum||"0")||0,mid:parseFloat(t.mid||t.midpoint||t.median||"0")||0,max:parseFloat(t.max||t.maximum||"0")||0})).filter(t=>t.label&&(t.min||t.max)),i=`<div style="color:var(--accent-green);font-size:12px;margin-bottom:8px">✓ Found ${r.length} salary grade(s) in "${escHtml(a)}"</div>\n      <div class="table-wrap" style="max-height:180px;overflow-y:auto">\n        <table><thead><tr><th>Grade</th><th>Min</th><th>Mid</th><th>Max</th></tr></thead>\n        <tbody>${r.map(t=>`<tr><td>${escHtml(t.label)}</td><td>$${t.min.toLocaleString()}</td><td>$${t.mid.toLocaleString()}</td><td>$${t.max.toLocaleString()}</td></tr>`).join("")}</tbody></table></div>`):"clusters"===e?(r=o.map(t=>({name:t.name||t.levelname||t.level||t.label||"",min:parseFloat(t.min||t.minimum||t.minpoints||"0")||0,max:parseFloat(t.max||t.maximum||t.maxpoints||"0")||0})).filter(t=>t.name&&(t.min||t.max)),i=`<div style="color:var(--accent-green);font-size:12px;margin-bottom:8px">✓ Found ${r.length} job level(s) in "${escHtml(a)}"</div>\n      <div class="table-wrap" style="max-height:180px;overflow-y:auto">\n        <table><thead><tr><th>Level Name</th><th>Min Points</th><th>Max Points</th></tr></thead>\n        <tbody>${r.map(t=>`<tr><td>${escHtml(t.name)}</td><td>${t.min}</td><td>${t.max}</td></tr>`).join("")}</tbody></table></div>`):"salarymatrix"===e&&(r=o.map((t,e)=>Object.values(t).map(t=>parseFloat(t)).filter(t=>!isNaN(t)&&t>0)).filter(t=>t.length>=2),i=`<div style="color:var(--accent-green);font-size:12px;margin-bottom:8px">✓ Found ${r.length} grade rows (${r[0]?.length||0} steps each) in "${escHtml(a)}"</div>\n      <div class="table-wrap" style="max-height:180px;overflow-y:auto">\n        <table><thead><tr><th>Grade</th><th>Step 1</th><th>Step 2</th><th>...</th><th>Last Step</th></tr></thead>\n        <tbody>${r.slice(0,6).map((t,e)=>`<tr><td>Grade ${e+1}</td><td>$${t[0].toLocaleString()}</td><td>$${t[1].toLocaleString()}</td><td class="text-dim">···</td><td>$${t[t.length-1].toLocaleString()}</td></tr>`).join("")}</tbody></table></div>`),0===r.length)return n.innerHTML='<div style="color:var(--accent-red);font-size:12px">✕ No valid rows parsed. Check column names match the template.</div>',void(s.style.display="none");pendingImportPayload={type:e,data:r},n.innerHTML=i,s.style.display="flex",s.textContent=`Import ${r.length} ${"positions"===e?"Position":"factors"===e?"Factor":"Grade"}${1!==r.length?"s":""}`}function confirmImport(){if(!pendingImportPayload)return;const{type:t,data:e}=pendingImportPayload;if("project"===t){if(!confirm("This will replace ALL current project data. Continue?"))return;return Object.assign(state,e),document.getElementById("project-name-display").textContent=state.projectName||"Untitled",markUnsaved(),renderAll(),closeModal("modal-import"),void toast("Project imported successfully","success")}if("positions"===t)state.positions.length>0&&!confirm(`Replace all ${state.positions.length} existing position(s) with ${e.length} imported? Click Cancel to append instead.`)?state.positions.push(...e):state.positions=e,invalidateScoreCache(),renderPositionsTable(),toast(`Imported ${e.length} position(s)`,"success");else if("factors"===t)state.factors.length>0&&!confirm(`Replace all ${state.factors.length} existing factor(s) with ${e.length} imported? Click Cancel to append instead.`)?state.factors.push(...e):(state.factors=e,state.evaluations={},invalidateScoreCache()),renderFactors(),toast(`Imported ${e.length} factor(s)`,"success");else if("clusters"===t)state.clusters=e,renderClustering(),toast(`Imported ${e.length} job level(s)`,"success");else if("salary"===t)state.salaryGrades.length>0&&!confirm(`Replace all ${state.salaryGrades.length} existing grade(s) with ${e.length} imported? Click Cancel to append instead.`)?state.salaryGrades.push(...e):state.salaryGrades=e,toast(`Imported ${e.length} salary grade(s)`,"success");else if("salarymatrix"===t){salaryMatrix=e,matrixConfig.numGrades=e.length,matrixConfig.numSteps=e[0]?.length||matrixConfig.numSteps,state.matrixConfig=matrixConfig;const t=t=>document.getElementById(t);t("sm-num-grades")&&(t("sm-num-grades").value=matrixConfig.numGrades),t("sm-num-steps")&&(t("sm-num-steps").value=matrixConfig.numSteps),t("sm-grade1-min")&&(t("sm-grade1-min").value=e[0]?.[0]||matrixConfig.grade1Min),renderImportedMatrix(),toast(`Imported ${e.length}-grade salary matrix`,"success")}markUnsaved(),closeModal("modal-import"),pendingImportPayload=null}const TEMPLATES={positions:{filename:"positions-template.csv",headers:["Title","Department","ReportsTo","Salary","Description"],rows:[["Software Engineer","Engineering","VP Engineering","90000","Develops and maintains software systems"],["HR Business Partner","Human Resources","CHRO","80000","Strategic HR support for business units"],["Marketing Manager","Marketing","CMO","95000","Leads marketing campaigns and strategy"]]},factors:{filename:"factors-template.csv",headers:["FactorName","Weight","Description","Level1Name","Level1Points","Level1Desc","Level2Name","Level2Points","Level2Desc","Level3Name","Level3Points","Level3Desc","Level4Name","Level4Points","Level4Desc"],rows:[["Knowledge & Education",25,"Required knowledge and education","Basic",15,"High school or equivalent","Intermediate",35,"Bachelor's degree","Advanced",60,"Master's degree","Expert",90,"PhD or exceptional expertise"],["Problem Solving",20,"Complexity of problems addressed","Routine",10,"Follows clear procedures","Defined",25,"Chooses from known solutions","Complex",50,"Develops new solutions","Strategic",80,"Defines new problems and strategies"]]},salary:{filename:"salary-grades-template.csv",headers:["Label","Min","Mid","Max"],rows:[["Grade 1","40000","55000","70000"],["Grade 2","65000","85000","105000"],["Grade 3","95000","120000","145000"],["Grade 4","130000","162500","195000"],["Grade 5","180000","225000","270000"]]},clusters:{filename:"job-levels-template.csv",headers:["Name","Min","Max"],rows:[["Level 1 - Entry",0,200],["Level 2 - Developing",201,400],["Level 3 - Proficient",401,650],["Level 4 - Senior",651,900],["Level 5 - Lead / Expert",901,1200]]},salarymatrix:{filename:"salary-matrix-template.csv",headers:["Grade","Step1_Min","Step2","Step3","Step4","Step5","Step6","Step7","Step8_Max"],rows:[["Grade 1","30000","31800","33708","35730","37874","40147","42556","45109"],["Grade 2","38600","40916","43371","45973","48731","51655","54755","58041"],["Grade 3","49400","52364","55506","58836","62366","66108","70075","74279"]]}};function downloadTemplate(t){const e=TEMPLATES[t];if(!e)return;downloadCSV([e.headers,...e.rows],e.filename),toast(`Downloaded ${t} template`,"success")}function downloadAllTemplatesXLSX(){if("undefined"==typeof XLSX)return void toast("XLSX library not loaded","error");const t=XLSX.utils.book_new();Object.entries(TEMPLATES).forEach(([e,a])=>{const n=XLSX.utils.aoa_to_sheet([a.headers,...a.rows]);XLSX.utils.book_append_sheet(t,n,e.charAt(0).toUpperCase()+e.slice(1))}),XLSX.writeFile(t,"jeb-import-templates.xlsx"),toast("Downloaded all templates as XLSX","success")}function exportRankingCSV(){const t=state.positions.map(t=>({...t,score:getPositionScore(t.id)})).filter(t=>t.score>0).sort((t,e)=>e.score-t.score),e=[["Rank","Title","Department","Reports To","Score","Level","Grade","Current Salary"]];t.forEach((t,a)=>{const n=getAssignedLevel(t.score),s=getAssignedGrade(n);e.push([a+1,t.title,t.department||"",t.reportsTo||"",t.score.toFixed(0),n?.name||"",s?.label||"",t.salary||""])}),downloadCSV(e,"job-evaluation-ranking.csv"),toast("Exported ranking CSV","success")}function exportRankingXLSX(){if("undefined"==typeof XLSX)return void toast("XLSX library not loaded","error");const t=state.positions.map(t=>({...t,score:getPositionScore(t.id)})).filter(t=>t.score>0).sort((t,e)=>e.score-t.score),e=XLSX.utils.book_new(),a=[["Rank","Title","Department","Reports To","Score","Level","Grade","Current Salary"]];t.forEach((t,e)=>{const n=getAssignedLevel(t.score),s=getAssignedGrade(n);a.push([e+1,t.title,t.department||"",t.reportsTo||"",+t.score.toFixed(0),n?.name||"",s?.label||"",t.salary||""])}),XLSX.utils.book_append_sheet(e,XLSX.utils.aoa_to_sheet(a),"Rankings");const n=[["Position","Department","Total Score",...state.factors.map(t=>t.name+" (×"+t.weight+")")]];t.forEach(t=>{const e=state.evaluations[t.id]||{},a=[t.title,t.department||"",+t.score.toFixed(0)];state.factors.forEach(t=>{const n=e[t.id],s=void 0!==n?(t.levels[n]?.points||0)*t.weight:0;a.push(s)}),n.push(a)}),XLSX.utils.book_append_sheet(e,XLSX.utils.aoa_to_sheet(n),"Factor Breakdown"),XLSX.writeFile(e,"job-evaluation-ranking.xlsx"),toast("Exported ranking XLSX","success")}function exportSalaryCSV(){const t=[["Position","Score","Level","Grade","Min","Mid","Max","Current Salary","Compa-Ratio"]];state.positions.forEach(e=>{const a=getPositionScore(e.id);if(a>0){const n=getAssignedLevel(a),s=getAssignedGrade(n),o=s&&s.mid&&e.salary?(e.salary/s.mid*100).toFixed(1)+"%":"";t.push([e.title,a.toFixed(0),n?.name||"",s?.label||"",s?.min||"",s?.mid||"",s?.max||"",e.salary||"",o])}}),downloadCSV(t,"salary-structure.csv"),toast("Exported salary CSV","success")}function exportSalaryXLSX(){if("undefined"==typeof XLSX)return void toast("XLSX library not loaded","error");const t=XLSX.utils.book_new(),e=[["Position","Score","Level","Grade","Min","Mid","Max","Current Salary","Compa-Ratio"]];state.positions.forEach(t=>{const a=getPositionScore(t.id);if(a>0){const n=getAssignedLevel(a),s=getAssignedGrade(n),o=s&&s.mid&&t.salary?+(t.salary/s.mid*100).toFixed(1):"";e.push([t.title,+a.toFixed(0),n?.name||"",s?.label||"",s?.min||"",s?.mid||"",s?.max||"",t.salary||"",o])}}),XLSX.utils.book_append_sheet(t,XLSX.utils.aoa_to_sheet(e),"Salary Structure");const a=[["Grade","Min Salary","Midpoint","Max Salary","Range Spread"]];state.salaryGrades.forEach(t=>{const e=t.min>0?((t.max-t.min)/t.min*100).toFixed(1)+"%":"";a.push([t.label,t.min,t.mid,t.max,e])}),XLSX.utils.book_append_sheet(t,XLSX.utils.aoa_to_sheet(a),"Grade Definitions"),XLSX.writeFile(t,"salary-structure.xlsx"),toast("Exported salary XLSX","success")}function exportFactorsCSV(){const t=Math.max(...state.factors.map(t=>t.levels.length),0),e=[];for(let a=1;a<=t;a++)e.push(`Level${a}Name`,`Level${a}Points`,`Level${a}Desc`);const a=[["FactorName","Weight","Description",...e]];state.factors.forEach(e=>{const n=[e.name,e.weight,e.description||""];for(let a=0;a<t;a++){const t=e.levels[a];n.push(t?.name||"",t?.points||"",t?.description||"")}a.push(n)}),downloadCSV(a,"factor-framework.csv"),toast("Exported factors CSV","success")}function exportFactorsXLSX(){if("undefined"==typeof XLSX)return void toast("XLSX library not loaded","error");const t=XLSX.utils.book_new(),e=Math.max(...state.factors.map(t=>t.levels.length),0),a=[];for(let t=1;t<=e;t++)a.push(`Level${t}Name`,`Level${t}Points`,`Level${t}Desc`);const n=[["FactorName","Weight","Description",...a]];state.factors.forEach(t=>{const a=[t.name,t.weight,t.description||""];for(let n=0;n<e;n++){const e=t.levels[n];a.push(e?.name||"",e?e.points:"",e?.description||"")}n.push(a)}),XLSX.utils.book_append_sheet(t,XLSX.utils.aoa_to_sheet(n),"Factors"),XLSX.writeFile(t,"factor-framework.xlsx"),toast("Exported factors XLSX","success")}function downloadCSV(t,e){const a=t.map(t=>t.map(t=>`"${String(t).replace(/"/g,'""')}"`).join(",")).join("\n"),n=new Blob([a],{type:"text/csv"}),s=document.createElement("a");s.href=URL.createObjectURL(n),s.download=e,s.click()}function downloadProjectJSON(){const t=new Blob([JSON.stringify(state,null,2)],{type:"application/json"}),e=document.createElement("a");e.href=URL.createObjectURL(t),e.download=(state.projectName||"project")+".jeb.json",e.click(),toast("Project downloaded","success")}function exportFullReport(){const t=state.positions.map(t=>({...t,score:getPositionScore(t.id)})).filter(t=>t.score>0).sort((t,e)=>e.score-t.score),e=`<!DOCTYPE html><html><head><title>Job Evaluation Report - ${escHtml(state.projectName)}</title>\n  <style>body{font-family:Arial,sans-serif;max-width:900px;margin:40px auto;color:#222;line-height:1.6}\n  h1{color:#1a3a6e;border-bottom:2px solid #4f8ef7;padding-bottom:10px}\n  h2{color:#2a4a8e;margin-top:30px}\n  table{width:100%;border-collapse:collapse;margin:16px 0}\n  th{background:#1a3a6e;color:#fff;padding:8px 12px;text-align:left;font-size:12px}\n  td{padding:8px 12px;border-bottom:1px solid #e0e0e0;font-size:13px}\n  tr:nth-child(even)td{background:#f5f8ff}\n  .score{font-weight:bold;color:#2a6adf}\n  @media print{body{margin:20px}}</style></head><body>\n  <h1>Job Evaluation Report</h1>\n  <p><strong>Project:</strong> ${escHtml(state.projectName)} &nbsp;|&nbsp; <strong>Date:</strong> ${(new Date).toLocaleDateString()}</p>\n  <h2>Evaluation Summary</h2>\n  <table><thead><tr><th>#</th><th>Position</th><th>Department</th><th>Score</th><th>Level</th><th>Grade</th></tr></thead><tbody>\n  ${t.map((t,e)=>{const a=getAssignedLevel(t.score),n=getAssignedGrade(a);return`<tr><td>${e+1}</td><td>${escHtml(t.title)}</td><td>${escHtml(t.department||"")}</td><td class="score">${t.score.toFixed(0)}</td><td>${a?.name||"—"}</td><td>${n?.label||"—"}</td></tr>`}).join("")}\n  </tbody></table>\n  <h2>Compensable Factors</h2>\n  <table><thead><tr><th>Factor</th><th>Weight</th><th>Levels</th></tr></thead><tbody>\n  ${state.factors.map(t=>`<tr><td>${escHtml(t.name)}</td><td>${t.weight}</td><td>${t.levels.map(t=>t.name+" ("+t.points+")").join(", ")}</td></tr>`).join("")}\n  </tbody></table>\n  </body></html>`,a=window.open("","_blank");a.document.write(e),a.document.close(),a.print()}function showDataMenu(){openModal("modal-export")}function saveProject(){try{state.schemaVersion=SCHEMA_VERSION,localStorage.setItem("jeb_project",JSON.stringify(state)),document.getElementById("save-indicator").classList.remove("unsaved"),state.unsaved=!1,toast("Project saved","success")}catch(t){toast("Save failed: "+t.message,"error")}}function loadProject(){try{const t=localStorage.getItem("jeb_project");if(!t)return void toast("No saved project found","error");Object.assign(state,JSON.parse(t)),document.getElementById("project-name-display").textContent=state.projectName||"Untitled",document.getElementById("save-indicator").classList.remove("unsaved"),renderAll(),toast("Project loaded","success")}catch(t){toast("Load failed: "+t.message,"error")}}function newProject(){if(state.unsaved&&!confirm("Start a new project? Unsaved changes will be lost."))return;const t=prompt("Project name:","New Evaluation Project");t&&(state={projectName:t||"Untitled Project",factors:[],positions:[],incumbents:[],evaluations:{},clusters:[],salaryGrades:[],unsaved:!1},document.getElementById("project-name-display").textContent=state.projectName,renderAll(),toast("New project created","success"))}function markUnsaved(){state.unsaved=!0,document.getElementById("save-indicator").classList.add("unsaved")}function renderAll(){renderFactors(),renderPositionsTable(),renderIncumbentsTable(),renderDashboard()}function applyClusterChanges(){markUnsaved(),renderClustering(),toast("Level definitions applied & saved","success")}let editingIncumbentId=null;function openAddIncumbentModal(t=null){editingIncumbentId=t,document.getElementById("modal-incumbent-title").textContent=t?"Edit Incumbent":"Add Incumbent";const e=document.getElementById("inc-position");if(e.innerHTML='<option value="">— Select Position —</option>'+state.positions.map(t=>`<option value="${t.id}">${escHtml(t.title)}${t.department?" ("+escHtml(t.department)+")":""}</option>`).join(""),t){const a=(state.incumbents||[]).find(e=>e.id===t);a&&(document.getElementById("inc-name").value=a.name||"",e.value=a.positionId||"",document.getElementById("inc-dept").value=a.department||"",document.getElementById("inc-reports-to").value=a.reportsTo||"",document.getElementById("inc-salary").value=a.salary||"",document.getElementById("inc-status").value=a.status||"Regular",document.getElementById("inc-notes").value=a.notes||"")}else document.getElementById("inc-name").value="",e.value="",document.getElementById("inc-dept").value="",document.getElementById("inc-reports-to").value="",document.getElementById("inc-salary").value="",document.getElementById("inc-status").value="Regular",document.getElementById("inc-notes").value="";openModal("modal-incumbent")}function saveIncumbent(){const t=document.getElementById("inc-name").value.trim(),e=document.getElementById("inc-position").value,a=parseFloat(document.getElementById("inc-salary").value)||0;if(!t)return void toast("Employee name is required","error");if(!e)return void toast("Please select a position","error");state.incumbents||(state.incumbents=[]);const n={name:t,positionId:e,department:document.getElementById("inc-dept").value.trim(),reportsTo:document.getElementById("inc-reports-to").value.trim(),salary:a,status:document.getElementById("inc-status").value,notes:document.getElementById("inc-notes").value.trim()};if(editingIncumbentId){const t=state.incumbents.find(t=>t.id===editingIncumbentId);t&&Object.assign(t,n)}else state.incumbents.push({id:uid(),...n});markUnsaved(),closeModal("modal-incumbent"),renderIncumbentsTable(),toast(editingIncumbentId?"Incumbent updated":"Incumbent added","success")}function deleteIncumbent(t){confirm("Delete this incumbent?")&&(state.incumbents=(state.incumbents||[]).filter(e=>e.id!==t),markUnsaved(),renderIncumbentsTable(),toast("Incumbent deleted","info"))}function renderIncumbentsTable(){state.incumbents||(state.incumbents=[]);const t=(document.getElementById("incumbent-search")?.value||"").toLowerCase(),e=state.incumbents.filter(e=>e.name.toLowerCase().includes(t)||(e.department||"").toLowerCase().includes(t)||(e.reportsTo||"").toLowerCase().includes(t)),a=document.getElementById("incumbents-tbody"),n=document.getElementById("incumbents-empty");if(!a)return;const s=document.getElementById("incumbents-count");if(s&&(s.textContent=`${e.length} incumbent${1!==e.length?"s":""}`),0===e.length)return a.innerHTML="",void(n&&(n.style.display="block"));n&&(n.style.display="none"),a.innerHTML=e.map(t=>{const e=state.positions.find(e=>e.id===t.positionId),a=e?getPositionScore(e.id):0,n=e?getAssignedLevel(a):null,s=n?getAssignedGrade(n):null;let o="—";return t.salary&&s&&s.mid&&(o=(t.salary/s.mid*100).toFixed(1)+"%"),`<tr>\n      <td style="font-weight:600">${escHtml(t.name)}</td>\n      <td>${e?escHtml(e.title):'<span class="text-dim">—</span>'}</td>\n      <td>${escHtml(t.department||"—")}</td>\n      <td style="color:var(--text-dim)">${escHtml(t.reportsTo||"—")}</td>\n      <td style="font-family:var(--font-mono)">${t.salary?fmtCurrency(t.salary):"—"}</td>\n      <td style="font-family:var(--font-mono);color:var(--accent-cyan)">${a>0?a.toFixed(0):"—"}</td>\n      <td>${s?`<span class="badge badge-gold">${escHtml(s.label||s.grade||"—")}</span>`:'<span class="text-dim">—</span>'}</td>\n      <td style="font-family:var(--font-mono);color:${t.salary&&s&&s.mid?t.salary/s.mid>=1?"var(--accent-green)":"var(--accent-red)":"var(--text-dim)"}">${o}</td>\n      <td>\n        <div class="inline-actions">\n          <button class="btn btn-ghost btn-icon btn-sm" onclick="openAddIncumbentModal('${t.id}')">✎</button>\n          <button class="btn btn-danger btn-icon btn-sm" onclick="deleteIncumbent('${t.id}')">✕</button>\n        </div>\n      </td>\n    </tr>`}).join("")}function exportIncumbentsCSV(){if(!state.incumbents||0===state.incumbents.length)return void toast("No incumbents to export","error");const t=[["Employee Name","Position","Department","Reports To","Salary","Status","Eval Score","Grade","Compa-Ratio","Notes"]];state.incumbents.forEach(e=>{const a=state.positions.find(t=>t.id===e.positionId),n=a?getPositionScore(a.id):0,s=a?getAssignedLevel(n):null,o=s?getAssignedGrade(s):null,r=e.salary&&o&&o.mid?(e.salary/o.mid*100).toFixed(1)+"%":"";t.push([e.name,a?.title||"",e.department||"",e.reportsTo||"",e.salary||"",e.status||"",n>0?n.toFixed(0):"",o?.label||o?.grade||"",r,e.notes||""])});const e=t.map(t=>t.map(t=>`"${String(t).replace(/"/g,'""')}"`).join(",")).join("\n"),a=document.createElement("a");a.href="data:text/csv;charset=utf-8,"+encodeURIComponent(e),a.download="incumbents.csv",a.click(),toast("Incumbents exported","success")}function dmSwitchUploadTab(t){document.getElementById("dm-upload-pane-csv").style.display="csv"===t?"":"none",document.getElementById("dm-upload-pane-project").style.display="project"===t?"":"none",document.getElementById("dm-tab-csv").classList.toggle("active","csv"===t),document.getElementById("dm-tab-project").classList.toggle("active","project"===t)}function dmHandleProjectFile(t){const e=t.target.files[0];e&&processDmProjectFile(e)}function dmHandleProjectDrop(t){t.preventDefault();const e=t.dataTransfer.files[0];e&&processDmProjectFile(e)}function processDmProjectFile(t){if(!t.name.endsWith(".json"))return void toast("Please select a .jeb.json file","error");const e=new FileReader;e.onload=t=>{try{const e=JSON.parse(t.target.result);if(!e.factors&&!e.positions)return void toast("File does not appear to be a valid project JSON","error");const a=document.getElementById("dm-project-status"),n=[e.projectName?`Project: <strong>${escHtml(e.projectName)}</strong>`:"",e.factors?.length?`${e.factors.length} factors`:"",e.positions?.length?`${e.positions.length} positions`:"",e.clusters?.length?`${e.clusters.length} levels`:"",e.salaryGrades?.length?`${e.salaryGrades.length} salary grades`:"",e.incumbents?.length?`${e.incumbents.length} incumbents`:""].filter(Boolean).join(" · ");a.style.display="",a.innerHTML=`<div style="background:var(--bg-raised);border:1px solid var(--border-mid);border-radius:var(--radius-sm);padding:10px 12px;font-size:12px;color:var(--text-secondary);margin-bottom:10px">${n}</div>\n        <button class="btn btn-primary" style="width:100%" onclick="confirmDmProjectImport(${JSON.stringify(JSON.stringify(e)).replace(/</g,"\\u003c")})">\n          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 .49-3.5"/></svg>\n          Replace All Data with This Project\n        </button>`}catch(t){toast("Failed to parse JSON: "+t.message,"error")}},e.readAsText(t)}function confirmDmProjectImport(t){if(confirm("This will replace ALL current project data. Are you sure?"))try{const e=JSON.parse(t);Object.assign(state,e),state.incumbents||(state.incumbents=[]),state.currencySymbol&&(currencySymbol=state.currencySymbol),document.getElementById("project-name-display").textContent=state.projectName||"Untitled",markUnsaved(),renderAll(),toast("Project imported successfully","success"),document.getElementById("dm-project-status").style.display="none",document.getElementById("dm-project-file-input").value=""}catch(t){toast("Import failed: "+t.message,"error")}}function loadSampleData(){confirm("Load sample data? This will replace any existing data.")&&(state.projectName="Tech Company Evaluation",state.factors=[{id:"f1",name:"Knowledge & Education",weight:25,description:"Required knowledge, education and training",levels:[{name:"Basic",points:15,description:"High school diploma"},{name:"Intermediate",points:35,description:"Bachelor's degree"},{name:"Advanced",points:60,description:"Master's degree or equivalent"},{name:"Expert",points:90,description:"PhD or exceptional expertise"}]},{id:"f2",name:"Problem Solving",weight:20,description:"Complexity of problems addressed",levels:[{name:"Routine",points:10,description:"Follows clear procedures"},{name:"Defined",points:25,description:"Chooses from known solutions"},{name:"Complex",points:50,description:"Develops new solutions"},{name:"Strategic",points:80,description:"Defines new problems and strategies"}]},{id:"f3",name:"Accountability",weight:20,description:"Impact and responsibility for outcomes",levels:[{name:"Individual",points:10,description:"Own work only"},{name:"Team",points:25,description:"Small team impact"},{name:"Department",points:50,description:"Department-wide impact"},{name:"Organization",points:80,description:"Company-wide impact"}]},{id:"f4",name:"Leadership",weight:20,description:"People management and leadership scope",levels:[{name:"None",points:5,description:"No direct reports"},{name:"Team Lead",points:20,description:"Leads small team 2-5"},{name:"Manager",points:45,description:"Manages a function 6-20"},{name:"Director+",points:75,description:"Manages managers, 20+"}]},{id:"f5",name:"Communication",weight:15,description:"Communication complexity and stakeholders",levels:[{name:"Internal Basic",points:10,description:"Routine internal comms"},{name:"Internal Advanced",points:25,description:"Cross-team collaboration"},{name:"External Basic",points:45,description:"Client and vendor contact"},{name:"External Strategic",points:70,description:"Executive and board level"}]}],state.positions=[{id:"p1",title:"Junior Developer",department:"Engineering",reportsTo:"Engineering Manager",salary:65e3,description:"Entry-level software developer"},{id:"p2",title:"Mid-Level Developer",department:"Engineering",reportsTo:"Engineering Manager",salary:9e4,description:"Software developer with 3-5 years experience"},{id:"p3",title:"Senior Developer",department:"Engineering",reportsTo:"VP Engineering",salary:125e3,description:"Lead software developer"},{id:"p4",title:"Engineering Manager",department:"Engineering",reportsTo:"VP Engineering",salary:145e3,description:"Manages engineering team"},{id:"p5",title:"VP Engineering",department:"Engineering",reportsTo:"CTO",salary:2e5,description:"VP of Engineering"},{id:"p6",title:"HR Coordinator",department:"Human Resources",reportsTo:"HR Manager",salary:55e3,description:"HR administrative support"},{id:"p7",title:"HR Manager",department:"Human Resources",reportsTo:"CHRO",salary:95e3,description:"Manages HR operations"},{id:"p8",title:"Marketing Analyst",department:"Marketing",reportsTo:"Marketing Manager",salary:7e4,description:"Data and campaign analyst"},{id:"p9",title:"Marketing Manager",department:"Marketing",reportsTo:"CMO",salary:11e4,description:"Manages marketing campaigns"},{id:"p10",title:"CFO",department:"Finance",reportsTo:"CEO",salary:25e4,description:"Chief Financial Officer"}],state.evaluations={p1:{f1:0,f2:0,f3:0,f4:0,f5:0},p2:{f1:1,f2:1,f3:0,f4:0,f5:1},p3:{f1:1,f2:2,f3:1,f4:1,f5:2},p4:{f1:1,f2:2,f3:2,f4:2,f5:2},p5:{f1:2,f2:3,f3:3,f4:3,f5:3},p6:{f1:0,f2:0,f3:0,f4:0,f5:0},p7:{f1:1,f2:1,f3:2,f4:2,f5:2},p8:{f1:1,f2:1,f3:0,f4:0,f5:1},p9:{f1:1,f2:2,f3:1,f4:2,f5:2},p10:{f1:2,f2:3,f3:3,f4:3,f5:3}},state.clusters=[{name:"Level 1",min:0,max:1500},{name:"Level 2",min:1501,max:3e3},{name:"Level 3",min:3001,max:5e3},{name:"Level 4",min:5001,max:7500},{name:"Level 5",min:7501,max:99999}],state.salaryGrades=[{label:"Grade 1",min:4e4,mid:55e3,max:7e4},{label:"Grade 2",min:65e3,mid:85e3,max:105e3},{label:"Grade 3",min:95e3,mid:12e4,max:145e3},{label:"Grade 4",min:13e4,mid:162500,max:195e3},{label:"Grade 5",min:18e4,mid:225e3,max:27e4}],document.getElementById("project-name-display").textContent=state.projectName,markUnsaved(),renderAll(),toast("Sample data loaded!","success"))}function uid(){return Math.random().toString(36).slice(2,10)}function escHtml(t){return String(t).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;")}function toast(t,e="info"){const a=document.createElement("div");a.className=`toast ${e}`;a.innerHTML=`<span style="color:${"success"===e?"var(--accent-green)":"error"===e?"var(--accent-red)":"var(--accent-blue)"}">${{success:"✓",error:"✕",info:"ℹ"}[e]||"•"}</span>${escHtml(t)}`,document.getElementById("toast-container").appendChild(a),setTimeout(()=>a.remove(),3e3)}const manifestData={name:"Job Evaluation Tool",short_name:"JET Pro",description:"Professional job evaluation tool using point-factor methodology",start_url:"./",display:"standalone",background_color:"#060910",theme_color:"#0a0e1a",icons:[{src:"data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 192 192'><rect width='192' height='192' fill='%230a0e1a'/><text y='.9em' font-size='150' x='20'>📊</text></svg>",sizes:"192x192",type:"image/svg+xml"}]},manifestBlob=new Blob([JSON.stringify(manifestData)],{type:"application/json"}),manifestURL=URL.createObjectURL(manifestBlob);if(document.querySelector('link[rel="manifest"]').href=manifestURL,"serviceWorker"in navigator){const t=new Blob(["\n    const CACHE = 'jeb-v1';\n    self.addEventListener('install', e => e.waitUntil(caches.open(CACHE).then(c => c.addAll(['./','./app.js']))));\n    self.addEventListener('fetch', e => e.respondWith(caches.match(e.request).then(r => r || fetch(e.request))));\n  "],{type:"application/javascript"}),e=URL.createObjectURL(t);navigator.serviceWorker.register(e).catch(()=>{})}function toggleWorkflowPanel(){const t=document.getElementById("workflow-panel");if(!t)return;const e="none"===t.style.display;t.style.display=e?"":"none"}function updateWorkflowState(){const t=state.factors.length>0,e=state.positions.length>0,a=state.positions.filter(t=>state.evaluations[t.id]&&Object.keys(state.evaluations[t.id]).length>0).length>0,n=state.clusters.length>0,s=state.salaryGrades&&state.salaryGrades.length>0,o=state.salaryMatrix&&state.salaryMatrix.length>0,r=s||o;let i=1;t&&(i=1),t&&e&&(i=2),a&&(i=3),a&&n&&(i=4),r&&(i=5),s&&(i=6);const l=[{id:1,done:t},{id:2,done:e},{id:3,done:a},{id:4,done:n},{id:5,done:r},{id:6,done:s},{id:7,done:!1}];l.forEach(e=>{const a=document.getElementById("wf-"+e.id);a&&(a.classList.remove("wf-done","wf-active","wf-locked"),e.done?a.classList.add("wf-done"):e.id===i||1===e.id&&!t?a.classList.add("wf-active"):e.id>i+1&&a.classList.add("wf-locked"),a.style.pointerEvents=e.done||e.id<=i+1?"auto":"none",a.style.opacity=e.done||e.id<=i+1?"1":"0.38")});const d=Math.round(l.filter(t=>t.done).length/(l.length-1)*100),c=document.getElementById("dash-progress-bar"),m=document.getElementById("dash-progress-pct");c&&(c.style.width=d+"%"),m&&(m.textContent=d+"% complete")}function updateEvalSummary(){if(!currentEvalPositionId)return;const t=document.getElementById("workspace-summary-area");t&&(t.style.display="block");state.positions.find(t=>t.id===currentEvalPositionId);const e=getPositionScore(currentEvalPositionId),a=getAssignedLevel(e),n=a?getAssignedGrade(a):null,s=document.getElementById("es-score"),o=document.getElementById("es-level"),r=document.getElementById("es-grade"),i=document.getElementById("es-range");s&&(s.textContent=e>0?e.toFixed(0):"—"),o&&(o.textContent=a?a.name:"—"),r&&(r.textContent=n?n.label:"—"),i&&(i.textContent=n?`${fmtCurrency(n.min)} → ${fmtCurrency(n.max)}`:"—"),updateFactorContrib(e)}function updateFactorContrib(t){const e=document.getElementById("factor-contrib-rows");if(!e||!currentEvalPositionId)return;const a=state.evaluations[currentEvalPositionId]||{},n=state.factors.map(e=>{const n=a[e.id],s=void 0!==n&&n>=0?(e.levels[n]?.points||0)*e.weight:0,o=t>0?s/t*100:0;return{name:e.name,pts:s,pct:o}}).filter(t=>t.pts>0).sort((t,e)=>e.pts-t.pts);0!==n.length?e.innerHTML=n.map(t=>`\n    <div style="display:grid;grid-template-columns:140px 1fr 50px 50px;gap:10px;align-items:center;padding:7px 0;border-bottom:1px solid var(--border-dim)">\n      <span style="font-size:12px;color:var(--text-primary);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${escHtml(t.name)}</span>\n      <div style="background:var(--bg-raised);border-radius:3px;height:10px;overflow:hidden;border:1px solid var(--border-dim)">\n        <div style="height:100%;background:var(--accent-cyan);width:${t.pct.toFixed(1)}%;border-radius:3px;transition:width .3s"></div>\n      </div>\n      <span style="font-family:var(--font-mono);font-size:10px;color:var(--accent-cyan);text-align:right">${t.pct.toFixed(1)}%</span>\n      <span style="font-family:var(--font-mono);font-size:10px;color:var(--text-secondary);text-align:right">${t.pts.toFixed(0)}</span>\n    </div>`).join(""):e.innerHTML='<div class="text-dim text-sm" style="padding:12px 0">No factor scores yet.</div>'}document.addEventListener("DOMContentLoaded",()=>{applyTheme();try{const t=localStorage.getItem("jeb_project");if(t&&(Object.assign(state,JSON.parse(t)),document.getElementById("project-name-display").textContent=state.projectName||"Untitled",state.currencySymbol)){currencySymbol=state.currencySymbol;const t=document.getElementById("currency-select");if(Array.from(t.options).map(t=>t.value).includes(currencySymbol))t.value=currencySymbol;else{let e=document.createElement("option");e.value="__custom__",e.textContent=currencySymbol+" (custom)",t.insertBefore(e,t.lastElementChild),t.value="__custom__"}}}catch(t){}renderFactors(),renderPositionsTable(),renderDashboard(),setInterval(()=>{state.unsaved&&saveProject()},6e4),document.addEventListener("keydown",t=>{"Escape"===t.key&&document.querySelectorAll(".modal-overlay.open").forEach(t=>t.classList.remove("open"))})});let _clusterDistChart=null;function renderClusterDistChart(){const t=document.getElementById("chart-cluster-dist");if(!t)return;const e=state.positions.map(t=>getPositionScore(t.id)).filter(t=>t>0);if(0===e.length)return void(t.parentElement.innerHTML='<div class="text-dim text-sm" style="padding:20px;text-align:center">No evaluated positions yet.</div>');const a=Math.min(...e),n=Math.max(...e),s=Math.max(1,Math.ceil((n-a+1)/12)),o=Array.from({length:12},(t,e)=>({label:Math.round(a+e*s),count:0,positions:[]}));e.forEach((t,e)=>{const n=Math.min(11,Math.floor((t-a)/s));o[n].count++,o[n].positions.push(state.positions[e]?.title||"")});const r={};state.clusters.forEach((t,e)=>{r["line"+e]={type:"line",scaleID:"x",value:t.min,borderColor:"rgba(245,200,66,0.7)",borderWidth:1,borderDash:[4,3],label:{enabled:!0,content:t.name,position:"start",color:"var(--accent-gold)",font:{size:9}}}}),_clusterDistChart&&(_clusterDistChart.destroy(),_clusterDistChart=null);const i=getChartColors();_clusterDistChart=new Chart(t,{type:"bar",data:{labels:o.map(t=>t.label),datasets:[{label:"Positions",data:o.map(t=>t.count),backgroundColor:"rgba(0,212,255,0.25)",borderColor:i.accent,borderWidth:1,borderRadius:2}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{display:!1},tooltip:{callbacks:{title:t=>`Score ~${t[0].label}`,label:t=>`${t.raw} position${1!==t.raw?"s":""}`}}},scales:{x:{ticks:{color:i.tick,font:{size:9}},grid:{color:i.grid},title:{display:!0,text:"Job Score",color:i.tick,font:{size:9}}},y:{ticks:{color:i.tick,font:{size:9},stepSize:1},grid:{color:i.grid},title:{display:!0,text:"Count",color:i.tick,font:{size:9}}}}}})}function autoGenerateClustersQuantile(){const t=state.positions.map(t=>getPositionScore(t.id)).filter(t=>t>0).sort((t,e)=>t-e);if(0===t.length)return void toast("No evaluated positions to cluster","error");const e=Math.min(7,Math.max(3,Math.round(t.length/2))),a=t.length/e;state.clusters=Array.from({length:e},(n,s)=>{const o=t[Math.floor(s*a)],r=s===e-1?t[t.length-1]+1:t[Math.floor((s+1)*a)];return{name:`Level ${s+1}`,min:Math.round(o),max:Math.round(r)}}),markUnsaved(),renderClustering(),toast(`Auto-generated ${e} levels from score quantiles`,"success")}let _socChart=null;function renderSpanOfControl(){if(0===state.positions.length)return document.getElementById("soc-stats").innerHTML='<div class="text-dim text-sm" style="grid-column:1/-1;padding:20px;text-align:center">No positions loaded. Import positions to analyse span of control.</div>',document.getElementById("soc-table-tbody").innerHTML="",void(document.getElementById("soc-depth-body").innerHTML="");const t={};state.positions.forEach(e=>{t[e.title?.trim().toLowerCase()]=e});const e={};state.positions.forEach(t=>{const a=t.reportsTo?.trim();if(!a)return;const n=a.toLowerCase();e[n]||(e[n]=[]),e[n].push(t)});const a=Object.entries(e).map(([e,a])=>{const n=t[e];return{title:n?n.title:a[0]?.reportsTo||e,dept:n?.department||"—",reports:a.length,flag:a.length>8?"over":a.length<2?"under":"ok"}}).sort((t,e)=>e.reports-t.reports),n=a.filter(t=>"over"===t.flag).length,s=a.filter(t=>"under"===t.flag).length,o=a.length?a.reduce((t,e)=>t+e.reports,0)/a.length:0;function r(t,e,a=0){const n=e[t?.toLowerCase()]||[];return 0===n.length?a:Math.max(...n.map(t=>r(t.title,e,a+1)))}const i=state.positions.filter(e=>!e.reportsTo||!t[e.reportsTo?.toLowerCase()]),l=i.length>0?Math.max(...i.map(t=>r(t.title,e))):0;document.getElementById("soc-stats").innerHTML=`\n    <div class="stat-card blue"><div class="stat-label">Total Managers</div><div class="stat-value">${a.length}</div><div class="stat-sub">With direct reports</div></div>\n    <div class="stat-card ${n>0?"red":"green"}"><div class="stat-label">Overloaded</div><div class="stat-value">${n}</div><div class="stat-sub">&gt;8 direct reports</div></div>\n    <div class="stat-card gold"><div class="stat-label">Avg Span</div><div class="stat-value">${o.toFixed(1)}</div><div class="stat-sub">Reports per manager</div></div>\n    <div class="stat-card cyan"><div class="stat-label">Org Depth</div><div class="stat-value">${l}</div><div class="stat-sub">Hierarchy levels</div></div>`,document.getElementById("soc-table-tbody").innerHTML=a.map(t=>{const e="over"===t.flag?`<span class="badge badge-red">⚠ Overloaded (${t.reports})</span>`:"under"===t.flag?`<span class="badge" style="background:rgba(245,200,66,0.15);color:var(--accent-gold)">Low (${t.reports})</span>`:`<span class="badge badge-green">OK (${t.reports})</span>`;return`<tr>\n      <td>${escHtml(t.title)}</td>\n      <td style="font-family:var(--font-mono);font-weight:700;color:${"over"===t.flag?"var(--accent-red)":"under"===t.flag?"var(--accent-gold)":"var(--accent-green)"}">${t.reports}</td>\n      <td>${escHtml(t.dept)}</td>\n      <td>${e}</td></tr>`}).join("")||'<tr><td colspan="4" class="text-dim" style="padding:20px;text-align:center">No managers found — check that positions have a "Reports To" field.</td></tr>';const d=document.getElementById("chart-soc");if(d){_socChart&&(_socChart.destroy(),_socChart=null);const t=getChartColors();_socChart=new Chart(d,{type:"bar",data:{labels:a.slice(0,15).map(t=>t.title.length>18?t.title.slice(0,16)+"…":t.title),datasets:[{label:"Direct Reports",data:a.slice(0,15).map(t=>t.reports),backgroundColor:a.slice(0,15).map(t=>"over"===t.flag?"rgba(255,79,107,0.6)":"under"===t.flag?"rgba(245,200,66,0.45)":"rgba(0,212,255,0.4)"),borderColor:a.slice(0,15).map(t=>"over"===t.flag?"rgba(255,79,107,1)":"under"===t.flag?"rgba(245,200,66,1)":"rgba(0,212,255,0.9)"),borderWidth:1,borderRadius:3}]},options:{responsive:!0,maintainAspectRatio:!1,indexAxis:"y",plugins:{legend:{display:!1},annotation:{annotations:{threshold:{type:"line",scaleID:"x",value:8,borderColor:"rgba(255,79,107,0.7)",borderWidth:1,borderDash:[4,3]}}}},scales:{x:{ticks:{color:t.tick,font:{size:9},stepSize:1},grid:{color:t.grid}},y:{ticks:{color:t.tick,font:{size:9}},grid:{color:t.grid}}}}})}document.getElementById("soc-depth-body").innerHTML=`\n    <p style="font-size:13px;color:var(--text-secondary);margin-bottom:8px">Organizational hierarchy depth: <strong style="color:var(--accent-cyan)">${l} levels</strong></p>\n    <div style="font-size:12px;color:var(--text-dim)">Top-level positions (no manager): ${i.map(t=>`<span class="badge badge-blue" style="margin:2px">${escHtml(t.title)}</span>`).join("")||"None found"}</div>\n    ${s>0?`<p style="font-size:12px;color:var(--accent-gold);margin-top:8px">⚠ ${s} manager${s>1?"s":""} with fewer than 2 direct reports — consider restructuring.</p>`:""}\n    ${n>0?`<p style="font-size:12px;color:var(--accent-red);margin-top:4px">⚠ ${n} manager${n>1?"s":""} with more than 8 direct reports — review workload.</p>`:""}`}let _compaChart=null,_salVsMidChart=null;function getCompaRatio(t,e){return t&&e&&e.mid&&0!==e.mid?t/e.mid:null}function getPositionRangeStatus(t,e){return t&&e?t<e.min?"below":t>e.max?"above":"within":null}function renderPayEquity(){const t=state.positions.filter(t=>t.salary&&t.salary>0);if(0===t.length)return document.getElementById("equity-stats").innerHTML='<div class="text-dim text-sm" style="grid-column:1/-1;padding:20px;text-align:center">No positions with salary data. Add salaries to positions to run pay equity analysis.</div>',document.getElementById("grade-spread-tbody").innerHTML="",void(document.getElementById("equity-flags-tbody").innerHTML="");const e=t.map(t=>{const e=getPositionScore(t.id),a=getAssignedLevel(e),n=a?getAssignedGrade(a):null,s=getCompaRatio(t.salary,n),o=getPositionRangeStatus(t.salary,n);return{...t,score:e,level:a,grade:n,compa:s,status:o}}),a=e.filter(t=>"below"===t.status).length,n=e.filter(t=>"above"===t.status).length,s=e.filter(t=>"within"===t.status).length,o=e.filter(t=>null!==t.compa).map(t=>t.compa),r=o.length?o.reduce((t,e)=>t+e,0)/o.length:0;document.getElementById("equity-stats").innerHTML=`\n    <div class="stat-card ${a>0?"red":"green"}"><div class="stat-label">Below Range</div><div class="stat-value">${a}</div><div class="stat-sub">Under grade minimum</div></div>\n    <div class="stat-card green"><div class="stat-label">Within Range</div><div class="stat-value">${s}</div><div class="stat-sub">In grade band</div></div>\n    <div class="stat-card ${n>0?"gold":"green"}"><div class="stat-label">Above Range</div><div class="stat-value">${n}</div><div class="stat-sub">Over grade maximum</div></div>\n    <div class="stat-card cyan"><div class="stat-label">Avg Compa-Ratio</div><div class="stat-value">${r?r.toFixed(2):"—"}</div><div class="stat-sub">Target = 1.00</div></div>`,document.getElementById("grade-spread-tbody").innerHTML=0===state.salaryGrades.length?'<tr><td colspan="6" class="text-dim" style="padding:16px;text-align:center">No salary grades defined.</td></tr>':state.salaryGrades.map((t,e)=>{const a=t.min>0?((t.max-t.min)/t.min*100).toFixed(1):"—",n=state.salaryGrades[e-1],s=n?((t.min-n.min)/n.min*100).toFixed(1)+"%":"—";return`<tr>\n          <td><span class="badge badge-gold">${escHtml(t.label)}</span></td>\n          <td style="font-family:var(--font-mono)">${fmtCurrency(t.min)}</td>\n          <td style="font-family:var(--font-mono)">${fmtCurrency(t.mid)}</td>\n          <td style="font-family:var(--font-mono)">${fmtCurrency(t.max)}</td>\n          <td style="font-family:var(--font-mono);color:var(--accent-cyan)">${a}%</td>\n          <td style="font-family:var(--font-mono);color:var(--text-dim)">${s}</td></tr>`}).join(""),document.getElementById("equity-flags-tbody").innerHTML=e.map(t=>{const e=t.grade?"below"===t.status?'<span class="badge badge-red">⬇ Below Range</span>':"above"===t.status?'<span class="badge badge-gold">⬆ Above Range</span>':'<span class="badge badge-green">✓ Within Range</span>':'<span class="badge">No Grade</span>',a=t.compa?t.compa<.8?"var(--accent-red)":t.compa>1.2?"var(--accent-gold)":"var(--accent-green)":"var(--text-dim)";return`<tr>\n      <td>${escHtml(t.title)}</td>\n      <td style="font-family:var(--font-mono)">${fmtCurrency(t.salary)}</td>\n      <td style="font-family:var(--font-mono)">${t.grade?fmtCurrency(t.grade.min):"—"}</td>\n      <td style="font-family:var(--font-mono)">${t.grade?fmtCurrency(t.grade.mid):"—"}</td>\n      <td style="font-family:var(--font-mono)">${t.grade?fmtCurrency(t.grade.max):"—"}</td>\n      <td style="font-family:var(--font-mono);color:${a}">${null!==t.compa?t.compa.toFixed(2):"—"}</td>\n      <td>${e}</td></tr>`}).join("")||'<tr><td colspan="7" class="text-dim" style="padding:16px;text-align:center">No positions with complete grade assignments.</td></tr>';const i=getChartColors(),l=document.getElementById("chart-compa-dist");if(l&&o.length>0){_compaChart&&(_compaChart.destroy(),_compaChart=null);const t=[.6,.7,.8,.9,1,1.1,1.2,1.3,1.4],e=Array(t.length-1).fill(0);o.forEach(a=>{for(let n=0;n<t.length-1;n++)if(a>=t[n]&&a<t[n+1]){e[n]++;break}}),_compaChart=new Chart(l,{type:"bar",data:{labels:t.slice(0,-1).map((e,a)=>`${e.toFixed(1)}–${t[a+1].toFixed(1)}`),datasets:[{label:"Positions",data:e,backgroundColor:t.slice(0,-1).map(t=>t<.9?"rgba(255,79,107,0.4)":t>1.1?"rgba(245,200,66,0.4)":"rgba(46,204,138,0.4)"),borderColor:t.slice(0,-1).map(t=>t<.9?"rgba(255,79,107,1)":t>1.1?"rgba(245,200,66,1)":"rgba(46,204,138,1)"),borderWidth:1,borderRadius:2}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{display:!1}},scales:{x:{ticks:{color:i.tick,font:{size:9}},grid:{color:i.grid}},y:{ticks:{color:i.tick,font:{size:9},stepSize:1},grid:{color:i.grid}}}}})}const d=document.getElementById("chart-salary-vs-mid");if(d){_salVsMidChart&&(_salVsMidChart.destroy(),_salVsMidChart=null);const t=e.filter(t=>t.grade).map(t=>({x:t.grade.mid,y:t.salary,label:t.title})),a=e.filter(t=>t.grade).map(t=>t.grade.mid),n=a.length?1.3*Math.max(...a):1e5;_salVsMidChart=new Chart(d,{type:"scatter",data:{datasets:[{label:"Salary vs Midpoint",data:t,backgroundColor:t.map((t,a)=>"below"===e.filter(t=>t.grade)[a]?.status?"rgba(255,79,107,0.7)":"above"===e.filter(t=>t.grade)[a]?.status?"rgba(245,200,66,0.7)":"rgba(46,204,138,0.7)"),pointRadius:5},{label:"Perfect Equity",type:"line",data:[{x:0,y:0},{x:n,y:n}],borderColor:"rgba(0,212,255,0.5)",borderDash:[6,4],borderWidth:1,pointRadius:0,fill:!1}]},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{display:!1},tooltip:{callbacks:{label:t=>t.raw.label||""}}},scales:{x:{ticks:{color:i.tick,font:{size:9},callback:t=>fmtCurrency(t)},grid:{color:i.grid},title:{display:!0,text:"Grade Midpoint",color:i.tick,font:{size:9}}},y:{ticks:{color:i.tick,font:{size:9},callback:t=>fmtCurrency(t)},grid:{color:i.grid},title:{display:!0,text:"Actual Salary",color:i.tick,font:{size:9}}}}}})}}function getRangeStatusBadge(t){const e=getAssignedLevel(getPositionScore(t.id)),a=e?getAssignedGrade(e):null,n=getPositionRangeStatus(t.salary,a);return n&&t.salary?"below"===n?'<span class="badge badge-red">⬇ Below</span>':"above"===n?'<span class="badge badge-gold">⬆ Above</span>':'<span class="badge badge-green">✓ In Range</span>':'<span class="badge">No Salary</span>'}const SCHEMA_VERSION="1.2.0";function printReport(){const t=document.getElementById("consulting-report-container");if(!t||t.querySelector(".empty-state"))return void toast("Generate the report first before printing.","error");const e=window.open("","_blank","width=900,height=700");e.document.write(`<!DOCTYPE html><html><head><meta charset="utf-8">\n<title>${escHtml(state.projectName||"Consulting Report")}</title>\n<style>\n  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }\n  body { font-family: 'Segoe UI', Arial, sans-serif; font-size: 13px; color: #1a1a2e; background: #fff; margin: 0; padding: 0; }\n  .report-doc { max-width: 860px; margin: 0 auto; padding: 32px 40px 60px; }\n  .report-cover { text-align: center; padding: 48px 24px; border: 1px solid #dde; border-radius: 8px; margin-bottom: 32px; background: #f8f8fc; }\n  .report-logo { font-family: 'Courier New', monospace; font-size: 28px; font-weight: 900; letter-spacing: 6px; color: #0ea5e9; margin-bottom: 16px; }\n  .report-title { font-size: 22px; font-weight: 700; color: #1a1a2e; margin-bottom: 6px; }\n  .report-subtitle { font-size: 14px; color: #555; margin-bottom: 12px; }\n  .report-meta { font-family: 'Courier New', monospace; font-size: 10px; color: #888; }\n  .report-section { margin-bottom: 28px; break-inside: avoid; }\n  .report-section-title { font-size: 13px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: #0ea5e9; border-bottom: 1px solid #dde; padding-bottom: 8px; margin-bottom: 14px; }\n  .report-grid-4 { display: grid; grid-template-columns: repeat(4,1fr); gap: 10px; }\n  .report-stat { background: #f8f8fc; border: 1px solid #dde; border-radius: 6px; padding: 12px 14px; text-align: center; }\n  .report-stat-val { font-family: 'Courier New', monospace; font-size: 22px; font-weight: 700; color: #1a1a2e; }\n  .report-stat-lbl { font-size: 10px; color: #888; margin-top: 2px; }\n  .report-stat-red   { border-left: 3px solid #ef4444; }\n  .report-stat-gold  { border-left: 3px solid #f59e0b; }\n  .report-stat-green { border-left: 3px solid #22c55e; }\n  .report-table { width: 100%; border-collapse: collapse; font-size: 12px; margin-bottom: 4px; }\n  .report-table th { background: #f8f8fc; padding: 8px 10px; text-align: left; font-size: 10px; text-transform: uppercase; letter-spacing: 0.08em; color: #888; border-bottom: 1px solid #dde; }\n  .report-table td { padding: 8px 10px; border-bottom: 1px solid #eee; color: #333; vertical-align: middle; }\n  .report-table tr:last-child td { border-bottom: none; }\n  .report-footer { display: flex; justify-content: space-between; font-family: 'Courier New', monospace; font-size: 10px; color: #888; border-top: 1px solid #dde; padding-top: 12px; margin-top: 24px; }\n  .report-cover { break-after: page; }\n  .badge { display: inline-block; padding: 2px 8px; border-radius: 99px; font-size: 10px; font-weight: 600; }\n  .badge-green { background: #dcfce7; color: #16a34a; }\n  .badge-blue  { background: #dbeafe; color: #1d4ed8; }\n  .badge-gold  { background: #fef9c3; color: #b45309; }\n  .badge-red   { background: #fee2e2; color: #dc2626; }\n  @media print {\n    body { margin: 0; }\n    .report-doc { max-width: 100%; padding: 0; }\n    .report-cover { break-after: page; }\n    .report-section { break-inside: avoid; }\n  }\n</style></head><body>\n${t.innerHTML}\n<script>window.onload = function(){ window.print(); }<\/script>\n</body></html>`),e.document.close()}function buildConsultingReport(){const t=document.getElementById("consulting-report-container"),e=state.positions.map(t=>({...t,score:getPositionScore(t.id)})).filter(t=>t.score>0).sort((t,e)=>e.score-t.score),a=state.positions.filter(t=>t.salary&&t.salary>0),n=a.map(t=>{const e=getAssignedLevel(getPositionScore(t.id)),a=e?getAssignedGrade(e):null;return{...t,grade:a,status:getPositionRangeStatus(t.salary,a),compa:getCompaRatio(t.salary,a)}}),s=n.filter(t=>"below"===t.status).length,o=n.filter(t=>"above"===t.status).length,r=n.filter(t=>"within"===t.status).length;t.innerHTML=`\n  <div class="report-doc">\n    <div class="report-cover">\n      <div class="report-logo">JET</div>\n      <div class="report-title">${escHtml(state.projectName)}</div>\n      <div class="report-subtitle">Job Evaluation &amp; Compensation Structure Report</div>\n      <div class="report-meta">Generated ${(new Date).toLocaleDateString("en-US",{year:"numeric",month:"long",day:"numeric"})} &nbsp;·&nbsp; Schema v${SCHEMA_VERSION}</div>\n    </div>\n\n    <div class="report-section">\n      <div class="report-section-title">1. Project Overview</div>\n      <div class="report-grid-4">\n        <div class="report-stat"><div class="report-stat-val">${state.factors.length}</div><div class="report-stat-lbl">Compensable Factors</div></div>\n        <div class="report-stat"><div class="report-stat-val">${state.positions.length}</div><div class="report-stat-lbl">Total Positions</div></div>\n        <div class="report-stat"><div class="report-stat-val">${e.length}</div><div class="report-stat-lbl">Evaluated Positions</div></div>\n        <div class="report-stat"><div class="report-stat-val">${state.clusters.length}</div><div class="report-stat-lbl">Job Levels Defined</div></div>\n      </div>\n    </div>\n\n    <div class="report-section">\n      <div class="report-section-title">2. Factor Framework</div>\n      <table class="report-table">\n        <thead><tr><th>#</th><th>Factor</th><th>Weight</th><th>Levels</th><th>Description</th></tr></thead>\n        <tbody>${state.factors.map((t,e)=>`<tr>\n          <td>${e+1}</td><td><strong>${escHtml(t.name)}</strong></td>\n          <td style="font-family:var(--font-mono)">${t.weight}%</td>\n          <td>${t.levels.length}</td>\n          <td style="color:var(--text-dim)">${escHtml(t.description||"—")}</td></tr>`).join("")||'<tr><td colspan="5" style="text-align:center;color:var(--text-dim)">No factors defined</td></tr>'}</tbody>\n      </table>\n    </div>\n\n    <div class="report-section">\n      <div class="report-section-title">3. Job Ranking Table</div>\n      <table class="report-table">\n        <thead><tr><th>#</th><th>Position</th><th>Department</th><th>Score</th><th>Level</th><th>Salary Grade</th><th>Pay Range</th></tr></thead>\n        <tbody>${e.map((t,e)=>{const a=getAssignedLevel(t.score),n=a?getAssignedGrade(a):null;return`<tr>\n            <td>${e+1}</td><td><strong>${escHtml(t.title)}</strong></td>\n            <td>${escHtml(t.department||"—")}</td>\n            <td style="font-family:var(--font-mono);color:var(--accent-cyan)">${t.score.toFixed(0)}</td>\n            <td>${a?`<span class="badge badge-purple">${escHtml(a.name)}</span>`:"—"}</td>\n            <td>${n?`<span class="badge badge-gold">${escHtml(n.label)}</span>`:"—"}</td>\n            <td style="font-family:var(--font-mono)">${n?`${fmtCurrency(n.min)} – ${fmtCurrency(n.max)}`:"—"}</td></tr>`}).join("")||'<tr><td colspan="7" style="text-align:center;color:var(--text-dim)">No evaluated positions</td></tr>'}</tbody>\n      </table>\n    </div>\n\n    <div class="report-section">\n      <div class="report-section-title">4. Salary Structure Summary</div>\n      ${0===state.salaryGrades.length?'<p style="color:var(--text-dim)">No salary grades defined.</p>':`\n      <table class="report-table">\n        <thead><tr><th>Grade</th><th>Minimum</th><th>Midpoint</th><th>Maximum</th><th>Range Spread</th></tr></thead>\n        <tbody>${state.salaryGrades.map(t=>{const e=t.min>0?((t.max-t.min)/t.min*100).toFixed(1)+"%":"—";return`<tr><td><span class="badge badge-gold">${escHtml(t.label)}</span></td>\n            <td style="font-family:var(--font-mono)">${fmtCurrency(t.min)}</td>\n            <td style="font-family:var(--font-mono)">${fmtCurrency(t.mid)}</td>\n            <td style="font-family:var(--font-mono)">${fmtCurrency(t.max)}</td>\n            <td style="font-family:var(--font-mono);color:var(--accent-cyan)">${e}</td></tr>`}).join("")}</tbody>\n      </table>`}\n    </div>\n\n    <div class="report-section">\n      <div class="report-section-title">5. Pay Equity Findings</div>\n      ${0===a.length?'<p style="color:var(--text-dim)">No salary data available for equity analysis.</p>':`\n      <div class="report-grid-4" style="margin-bottom:16px">\n        <div class="report-stat ${s>0?"report-stat-red":"report-stat-green"}"><div class="report-stat-val">${s}</div><div class="report-stat-lbl">Below Grade Min</div></div>\n        <div class="report-stat report-stat-green"><div class="report-stat-val">${r}</div><div class="report-stat-lbl">Within Range</div></div>\n        <div class="report-stat ${o>0?"report-stat-gold":"report-stat-green"}"><div class="report-stat-val">${o}</div><div class="report-stat-lbl">Above Grade Max</div></div>\n        <div class="report-stat"><div class="report-stat-val">${a.length}</div><div class="report-stat-lbl">Positions Analysed</div></div>\n      </div>\n      <table class="report-table">\n        <thead><tr><th>Position</th><th>Salary</th><th>Grade</th><th>Compa-Ratio</th><th>Status</th></tr></thead>\n        <tbody>${n.map(t=>{const e=t.grade?"below"===t.status?'<span class="badge badge-red">Below Range</span>':"above"===t.status?'<span class="badge badge-gold">Above Range</span>':'<span class="badge badge-green">Within Range</span>':'<span class="badge">No Grade</span>';return`<tr>\n            <td>${escHtml(t.title)}</td>\n            <td style="font-family:var(--font-mono)">${fmtCurrency(t.salary)}</td>\n            <td>${t.grade?escHtml(t.grade.label):"—"}</td>\n            <td style="font-family:var(--font-mono)">${null!==t.compa?t.compa.toFixed(2):"—"}</td>\n            <td>${e}</td></tr>`}).join("")}</tbody>\n      </table>`}\n    </div>\n\n<div class="report-footer">\n  <span>${escHtml(state.projectName)}</span>\n  <span>Job Evaluation Tool · Developed and Created by Paul Alay · 2026</span>\n  <span>${(new Date).toLocaleDateString()}</span>\n</div>\n  </div>`,toast("Report generated","success")}let dmParsedData=null,dmValidErrors=[],dmCurrentType="factors";const DM_COL_HINTS={factors:'Required: <span style="color:var(--accent-cyan)">FactorName</span>, Weight &nbsp;·&nbsp; Levels: Level1Name, Level1Points, Level1Desc … (auto-detects up to Level8+)',positions:'Required: <span style="color:var(--accent-cyan)">Title</span> &nbsp;·&nbsp; Optional: Department, ReportsTo, Salary, Description',clusters:'Required: <span style="color:var(--accent-cyan)">Name</span>, Min, Max &nbsp;·&nbsp; Each row = one job level band',salary:'Required: <span style="color:var(--accent-cyan)">Label</span>, Min, Mid, Max',salarymatrix:"Row 1 = headers (Grade, Step1_Min, Step2 … StepN_Max) &nbsp;·&nbsp; Each row = one grade &nbsp;·&nbsp; Steps auto-detected"};function dmUpdateColHint(){const t=document.getElementById("dm-import-type")?.value||"factors",e=document.getElementById("dm-col-hint");e&&(e.innerHTML=DM_COL_HINTS[t]||"")}function dmResetUpload(){dmParsedData=null,dmValidErrors=[];const t=document.getElementById("dm-validation-area");t&&(t.style.display="none");const e=document.getElementById("dm-status-body");e&&(e.innerHTML='<div class="empty-state" style="padding:30px 10px"><div class="empty-state-title" style="font-size:13px">No file loaded</div><div class="empty-state-desc" style="font-size:11px">Upload a file to see validation results</div></div>');const a=document.getElementById("dm-status-badge");a&&(a.innerHTML=""),["dm-merge-btn","dm-replace-btn","dm-dl-errors-btn"].forEach(t=>{const e=document.getElementById(t);e&&(e.style.display="none")});const n=document.getElementById("dm-file-input");n&&(n.value="")}function dmHandleDrop(t){t.preventDefault(),document.getElementById("dm-drop-zone").style.borderColor="";const e=t.dataTransfer.files[0];e&&dmProcessFile(e)}function dmHandleFile(t){const e=t.target.files[0];e&&dmProcessFile(e),t.target.value=""}function dmProcessFile(t){dmCurrentType=document.getElementById("dm-import-type").value;const e=t.name.split(".").pop().toLowerCase(),a=new FileReader,n=document.getElementById("dm-status-body");n.innerHTML='<div style="padding:16px;color:var(--text-dim);font-size:12px">⏳ Reading file…</div>',"xlsx"===e||"xls"===e?(a.onload=e=>{try{const a=XLSX.read(new Uint8Array(e.target.result),{type:"array"}),n=a.Sheets[a.SheetNames[0]];dmValidateAndPreview(XLSX.utils.sheet_to_json(n,{defval:""}),t.name)}catch(t){n.innerHTML=`<div style="color:var(--accent-red);font-size:12px;padding:12px">✕ Could not read XLSX: ${escHtml(t.message)}</div>`}},a.readAsArrayBuffer(t)):(a.onload=e=>{dmValidateAndPreview(parseCSVToObjects(e.target.result),t.name)},a.readAsText(t))}function dmValidateAndPreview(t,e){const a=dmCurrentType;if(dmValidErrors=[],dmParsedData=null,!t||0===t.length)return dmShowStatus("error","✕ File is empty or has no data rows."),document.getElementById("dm-validation-area").style.display="",document.getElementById("dm-errors-body").innerHTML='<div style="color:var(--accent-red);font-size:12px;padding:8px">No data rows found. Check the file format.</div>',document.getElementById("dm-preview-table").innerHTML="",void(document.getElementById("dm-preview-count").textContent="0 rows");const n=t.map(t=>{const e={};return Object.keys(t).forEach(a=>{e[normalizeKey(a)]=t[a]}),e});"factors"===a?dmValidateFactors(n,e):"positions"===a?dmValidatePositions(n,e):"clusters"===a?dmValidateClusters(n,e):"salary"===a?dmValidateSalary(n,e):"salarymatrix"===a&&dmValidateSalaryMatrix(n,e)}function dmAddError(t,e,a){dmValidErrors.push({row:t,field:e,message:a})}function dmValidateFactors(t,e){const a=[],n={};t.forEach((t,e)=>{const s=e+2,o=String(t.factorname||t.factor||t.name||"").trim(),r=parseFloat(t.weight||"0");if(!o)return void dmAddError(s,"FactorName","Missing required FactorName");t.hasOwnProperty("weight")||void 0!==t.weight||dmAddError(s,"Weight","Missing Weight column"),(isNaN(r)||r<=0)&&dmAddError(s,"Weight",`Invalid numeric value "${t.weight}" in Weight — must be a positive number`),n[o.toLowerCase()]&&dmAddError(s,"FactorName",`Duplicate factor name: "${o}"`),n[o.toLowerCase()]=!0;const i=Object.keys(t),l=[];for(let e=1;e<=20;e++){const a=i.find(t=>t===`level${e}name`||t===`l${e}name`);if(!a)break;const n=String(t[a]||"").trim();if(!n)break;const r=i.find(t=>t===`level${e}points`||t===`l${e}points`),d=i.find(t=>t===`level${e}desc`||t===`level${e}description`),c=parseFloat(t[r]||"0");r&&""!==t[r]&&isNaN(c)&&dmAddError(s,`Level${e}Points`,`Invalid points "${t[r]}" for Level ${e} of "${o}"`),l.push({name:n,points:isNaN(c)?0:c,description:String(t[d]||"").trim()})}0!==l.length?a.push({id:uid(),name:o,weight:isNaN(r)?10:r,description:String(t.description||t.desc||"").trim(),levels:l}):dmAddError(s,"Level1Name",`Factor "${o}" has no levels — add Level1Name column`)}),dmFinishValidation(a,e,a.slice(0,8).map(t=>`<tr><td>${escHtml(t.name)}</td><td style="font-family:var(--font-mono)">${t.weight}</td><td>${t.levels.length}</td><td style="font-size:10px;color:var(--text-dim)">${escHtml(t.levels.map(t=>t.name).join(", "))}</td></tr>`),["Factor","Weight","# Levels","Level Names"])}function dmValidatePositions(t,e){const a=[],n={},s=t.map(t=>String(t.title||t.positiontitle||t.name||"").trim().toLowerCase()).filter(Boolean);t.forEach((t,e)=>{const o=e+2,r=String(t.title||t.positiontitle||t.name||"").trim(),i=String(t.reportsto||t.manager||t.supervisor||"").trim(),l=String(t.salary||t.currentsal||"").trim();r?(n[r.toLowerCase()]&&dmAddError(o,"Title",`Duplicate position title: "${r}"`),n[r.toLowerCase()]=!0,i&&!s.includes(i.toLowerCase())&&dmAddError(o,"ReportsTo",`ReportsTo "${i}" does not match any Title in this file`),l&&isNaN(parseFloat(l))&&dmAddError(o,"Salary",`Invalid numeric value "${l}" in Salary`),a.push({id:uid(),title:r,department:String(t.department||t.dept||"").trim(),reportsTo:i,salary:parseFloat(l)||null,description:String(t.description||t.desc||"").trim()})):dmAddError(o,"Title","Missing required Title")}),dmFinishValidation(a,e,a.slice(0,8).map(t=>`<tr><td>${escHtml(t.title)}</td><td>${escHtml(t.department||"—")}</td><td>${escHtml(t.reportsTo||"—")}</td><td>${t.salary?fmtCurrency(t.salary):"—"}</td></tr>`),["Title","Department","Reports To","Salary"])}function dmValidateClusters(t,e){const a=[];t.forEach((t,e)=>{const n=e+2,s=String(t.name||t.levelname||t.level||t.label||"").trim(),o=parseFloat(t.min||t.minimum||t.minpoints||"0"),r=parseFloat(t.max||t.maximum||t.maxpoints||"0");s?(isNaN(o)&&dmAddError(n,"Min",`Invalid numeric value "${t.min}" in Min`),isNaN(r)&&dmAddError(n,"Max",`Invalid numeric value "${t.max}" in Max`),!isNaN(o)&&!isNaN(r)&&o>=r&&dmAddError(n,"Range",`Min (${o}) must be less than Max (${r}) for "${s}"`),a.push({name:s,min:isNaN(o)?0:o,max:isNaN(r)?0:r})):dmAddError(n,"Name","Missing required Name")}),dmFinishValidation(a,e,a.map(t=>`<tr><td>${escHtml(t.name)}</td><td style="font-family:var(--font-mono)">${t.min}</td><td style="font-family:var(--font-mono)">${t.max}</td></tr>`),["Level Name","Min Points","Max Points"])}function dmValidateSalary(t,e){const a=[];t.forEach((t,e)=>{const n=e+2,s=String(t.label||t.grade||t.gradename||"").trim(),o=parseFloat(t.min||t.minimum||"0"),r=parseFloat(t.mid||t.midpoint||t.median||"0"),i=parseFloat(t.max||t.maximum||"0");s?(isNaN(o)&&dmAddError(n,"Min",`Invalid numeric value "${t.min}" in Min`),isNaN(r)&&dmAddError(n,"Mid",`Invalid numeric value "${t.mid}" in Mid`),isNaN(i)&&dmAddError(n,"Max",`Invalid numeric value "${t.max}" in Max`),!isNaN(o)&&!isNaN(i)&&o>i&&dmAddError(n,"Min/Max",`Min (${o}) exceeds Max (${i}) for "${s}"`),a.push({label:s,min:isNaN(o)?0:o,mid:isNaN(r)?0:r,max:isNaN(i)?0:i})):dmAddError(n,"Label","Missing required Label")}),dmFinishValidation(a,e,a.map(t=>`<tr><td>${escHtml(t.label)}</td><td style="font-family:var(--font-mono)">${fmtCurrency(t.min)}</td><td style="font-family:var(--font-mono)">${fmtCurrency(t.mid)}</td><td style="font-family:var(--font-mono)">${fmtCurrency(t.max)}</td></tr>`),["Grade","Min","Mid","Max"])}function dmValidateSalaryMatrix(t,e){const a=[];t.forEach((t,e)=>{const n=e+2,s=Object.keys(t).slice(1).map(e=>parseFloat(t[e])).filter(t=>!isNaN(t)&&t>0);if(s.length<2)dmAddError(n,"Steps",`Row ${e+1} has fewer than 2 valid numeric step values`);else{for(let t=1;t<s.length;t++)s[t]<s[t-1]&&dmAddError(n,`Step${t+1}`,`Step ${t+1} (${s[t]}) is less than Step ${t} (${s[t-1]}) — steps should increase`);a.push(s)}}),dmFinishValidation(a,e,a.slice(0,6).map((t,e)=>`<tr><td>Grade ${e+1}</td><td style="font-family:var(--font-mono)">${fmtCurrency(t[0])}</td><td style="color:var(--text-dim)">···</td><td style="font-family:var(--font-mono)">${fmtCurrency(t[t.length-1])}</td><td style="font-family:var(--font-mono);font-size:10px;color:var(--text-dim)">${t.length}</td></tr>`),["Grade","Step 1 (Min)","…","Last Step (Max)","# Steps"])}function dmFinishValidation(t,e,a,n){const s=dmValidErrors.length,o=t.length,r=document.getElementById("dm-status-badge");r&&(r.innerHTML=0===s?`<span class="badge badge-green">✓ ${o} valid</span>`:`<span class="badge badge-red">⚠ ${s} error${1!==s?"s":""}</span>`),0===s?dmShowStatus("success",`✓ <strong>${o} rows</strong> validated from <em>${escHtml(e)}</em>. Choose import mode below.`):dmShowStatus("warning",`⚠ <strong>${s} validation error${1!==s?"s":""}</strong>. Fix issues or proceed with ${o} valid rows.`);const i=document.getElementById("dm-errors-body"),l=document.getElementById("dm-dl-errors-btn");0===s?(i.innerHTML='<div style="color:var(--accent-green);font-size:12px;padding:8px 0">✓ All rows passed validation.</div>',l&&(l.style.display="none")):(l&&(l.style.display=""),i.innerHTML=`<div class="table-wrap" style="max-height:200px;overflow-y:auto">\n      <table><thead><tr><th>Row</th><th>Field</th><th>Issue</th></tr></thead>\n      <tbody>${dmValidErrors.map(t=>`<tr>\n        <td style="font-family:var(--font-mono)">${t.row}</td>\n        <td style="color:var(--accent-gold)">${escHtml(t.field)}</td>\n        <td style="color:var(--accent-red);font-size:11px">${escHtml(t.message)}</td>\n      </tr>`).join("")}</tbody></table></div>`);const d=document.getElementById("dm-preview-count"),c=document.getElementById("dm-preview-table");d&&(d.textContent=`${o} rows`),c&&(c.innerHTML=0===o?'<div style="padding:16px;color:var(--text-dim);font-size:12px">No valid rows to preview.</div>':`<table><thead><tr>${n.map(t=>`<th>${escHtml(t)}</th>`).join("")}</tr></thead>\n       <tbody>${a.join("")}${o>a.length?`<tr><td colspan="${n.length}" style="color:var(--text-dim);font-size:11px;padding:8px">… and ${o-a.length} more rows</td></tr>`:""}</tbody></table>`),dmParsedData=t.length>0?t:null,document.getElementById("dm-validation-area").style.display="";const m=dmHasExistingData(dmCurrentType),p=document.getElementById("dm-merge-btn"),u=document.getElementById("dm-replace-btn");dmParsedData?(p&&(p.style.display=m?"":"none"),u&&(u.style.display="",u.textContent=m?"↺ Replace Existing":"✓ Import Data")):(p&&(p.style.display="none"),u&&(u.style.display="none")),renderDataManagement()}function dmHasExistingData(t){return"factors"===t?state.factors.length>0:"positions"===t?state.positions.length>0:"clusters"===t?state.clusters.length>0:"salary"===t?state.salaryGrades.length>0:"salarymatrix"===t&&!!(state.salaryMatrix&&state.salaryMatrix.length>0)}function dmShowStatus(t,e){const a=document.getElementById("dm-status-body");if(!a)return;a.innerHTML=`<div style="padding:12px;font-size:12px;color:${{success:"var(--accent-green)",warning:"var(--accent-gold)",error:"var(--accent-red)"}[t]||"var(--text-secondary)"};line-height:1.6">${e}</div>`}function dmDownloadErrors(){if(!dmValidErrors.length)return;downloadCSV([["Row","Field","Error Message"],...dmValidErrors.map(t=>[t.row,t.field,t.message])],"import-validation-errors.csv"),toast("Downloaded validation errors CSV","success")}function dmDoImport(t){if(!dmParsedData||0===dmParsedData.length)return void toast("No valid data to import","error");const e=dmCurrentType,a=dmParsedData.length,n={factors:"factor(s)",positions:"position(s)",clusters:"job level(s)",salary:"salary grade(s)",salarymatrix:"matrix row(s)"},s={factors:state.factors.length,positions:state.positions.length,clusters:state.clusters.length,salary:state.salaryGrades.length,salarymatrix:(state.salaryMatrix||[]).length};if("replace"===t&&dmHasExistingData(e)&&!confirm(`⚠ Replace all ${s[e]} existing ${n[e]||"records"} with ${a} imported?\n\nThis cannot be undone.`))return;if("factors"===e)"replace"===t?(state.factors=dmParsedData,state.evaluations={},invalidateScoreCache()):dmParsedData.forEach(t=>{const e=state.factors.findIndex(e=>e.name.toLowerCase()===t.name.toLowerCase());e>=0?(state.factors[e]={...state.factors[e],...t},invalidateScoreCache()):state.factors.push(t)}),renderFactors();else if("positions"===e)"replace"===t?(state.positions=dmParsedData,invalidateScoreCache()):dmParsedData.forEach(t=>{const e=state.positions.findIndex(e=>e.title.toLowerCase()===t.title.toLowerCase());e>=0?(state.positions[e]={...state.positions[e],...t},invalidateScoreCache(state.positions[e].id)):state.positions.push(t)}),renderPositionsTable();else if("clusters"===e)state.clusters="replace"===t?dmParsedData:[...state.clusters,...dmParsedData.filter(t=>!state.clusters.find(e=>e.name.toLowerCase()===t.name.toLowerCase()))],renderClustering();else if("salary"===e)"replace"===t?state.salaryGrades=dmParsedData:dmParsedData.forEach(t=>{const e=state.salaryGrades.findIndex(e=>e.label.toLowerCase()===t.label.toLowerCase());e>=0?state.salaryGrades[e]={...state.salaryGrades[e],...t}:state.salaryGrades.push(t)});else if("salarymatrix"===e){if("replace"===t)state.salaryMatrix=dmParsedData;else{const t=[...state.salaryMatrix||[]];dmParsedData.forEach((e,a)=>{t[a]=e}),state.salaryMatrix=t}salaryMatrix=state.salaryMatrix,matrixConfig.numGrades=state.salaryMatrix.length,matrixConfig.numSteps=state.salaryMatrix[0]?.length||matrixConfig.numSteps,state.matrixConfig=matrixConfig}markUnsaved(),updateWorkflowState(),dmShowStatus("success",`✓ <strong>${a} ${e}</strong> imported (${t} mode). Data is ready.`);const o=document.getElementById("dm-merge-btn"),r=document.getElementById("dm-replace-btn");o&&(o.style.display="none"),r&&(r.style.display="none"),dmParsedData=null,renderDataManagement(),toast(`✓ ${a} ${e} imported (${t})`,"success")}function renderDataManagement(){dmUpdateColHint();const t=document.getElementById("dm-data-summary");if(!t)return;const e=state.positions.filter(t=>state.evaluations[t.id]&&Object.keys(state.evaluations[t.id]).length>0).length;t.innerHTML=`\n    <div class="stat-card blue"><div class="stat-label">Factors</div><div class="stat-value">${state.factors.length}</div><div class="stat-sub">Compensable factors</div></div>\n    <div class="stat-card cyan"><div class="stat-label">Positions</div><div class="stat-value">${state.positions.length}</div><div class="stat-sub">${e} evaluated</div></div>\n    <div class="stat-card gold"><div class="stat-label">Job Levels</div><div class="stat-value">${state.clusters.length}</div><div class="stat-sub">Level bands</div></div>\n    <div class="stat-card green"><div class="stat-label">Salary Grades</div><div class="stat-value">${state.salaryGrades.length}</div><div class="stat-sub">Grade definitions</div></div>\n    <div class="stat-card blue"><div class="stat-label">Matrix Rows</div><div class="stat-value">${(state.salaryMatrix||[]).length}</div><div class="stat-sub">${(state.salaryMatrix?.[0]||[]).length?state.salaryMatrix[0].length+" steps":"Not loaded"}</div></div>`}
